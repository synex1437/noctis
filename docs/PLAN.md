# noctis — Tasarım ve Hata Analizi

> Turkish, author's working notes: the design goals, the architecture and the numbered bug log (#1–#270) of every release. The English reference is [REFERENCE.md](REFERENCE.md); the host adapters are in [HOSTS.md](HOSTS.md).

## 1. Hedefler

1. 5 saatlik pencere %92, haftalık (tüm modeller) %95 → işi unutmadan durdur/beklet, sıfırlanınca alarm + kaldığı yerden devam.
2. Haftalık Fable %97 → Opus 5 (effort max) yeni varsayılan olsun ve aktif iş Opus ile sürsün.
3. Kendi limitini doğru takip etsin (resmi veri), sıfırlanma saatini bilsin.
4. Token maliyeti fark edilemez olsun; kodsuz araştırma işleri Opus 5 alt-ajanına gitsin, kodlama daima Fable 5.1 max.
5. Sessiz ölüm ya da yanlış davranış olmasın; her hata log'a ve mümkünse kullanıcıya bildirime düşsün.

## 2. Mimari

```
Claude Code ──statusLine JSON (her asistan mesajı)──▶ noctis statusline ──▶ usage.json   (0 token)
            ──hook: UserPromptSubmit / PostToolBatch──▶ noctis hook ──▶ karar
                       │  eşik yok → sessiz çıkış (0 token)
                       │  kısa bekleme (≤330 dk) → hook içinde uyu → tek satır systemMessage ile dön (bağlam korunur)
                       │  uzun bekleme → checkpoint + dur + Görev Zamanlayıcı (resume)
                       │  Fable ≥%97 → settings.model=opus (+ yeniden başlatma planı)
            ──hook: StopFailure(rate_limit) / Notification(quota_auto_resume_*)──▶ yedek plan / iptal
            ──hook: PostModelSwitch──▶ oturumun canlı modeli (Fable kuralı için)
            ──hook: SessionStart(startup)──▶ yetim checkpoint varsa 2 satırlık işaretçi

Görev Zamanlayıcı / launchd / systemd ──▶ noctis resume --sid X --account D [--host codex|antigravity|droid|copilot]
   ├─ limit hâlâ aktif → yeniden planla
   ├─ oturum bu arada devam etmiş (transcript mtime > reset / dahili auto-continue) → hiçbir şey yapma
   └─ alarm + claude --resume X --model <m> --effort max --permission-mode <pm> -- "<prompt>"  (yeni pencere)

Codex CLI / Antigravity CLI / Droid / Copilot CLI ──hook dosyaları──▶ aynı `noctis hook --host <araç>` ──▶ host.go çevirir ──▶ aynı karar motoru
```

Dosyalar (`<hesap>/noctis/`): `config.json`, `usage.json` (yalnızca statusline yazar), `fable.json` (yalnızca OAuth yoklayıcı yazar), `state.json` (hook/runner, kilit dosyasıyla), `checkpoints/<sid>.md`, `launches/<sid>.json` (geçici), `guard.log` (512 KB rotasyon), `resume-output.log`.

Tek yazar ilkesi: her dosyanın tek bir yazar süreci var; `state.json` için `wx` kilit dosyası + atomik yazma (tmp + rename).

## 3. Olay akışları

**A. 5 saat %92, VS Code açık.** PostToolBatch → checkpoint (transcript'ten, model çağrısı yok) → `waits[sid]` + izleyici görevi (reset+90 s+180 s) → devam anına en fazla 330 dk varsa (`wait.maxInHookMinutes`) hook `resets_at + 90 s`'ye kadar 15 s'lik dilimlerle uyur (duvar saati; PC uykusuna dayanıklı); her dilimde kalp atışı yazar, 5 dk'da bir kullanımı tazeler (`wait.earlyResetPollMinutes`) ve pencere erken sıfırlandıysa beklemeyi o an bitirir → bekleme kaydı silinir, checkpoint tüketilir, izleyici iptal edilir, masaüstü bildirimi (+ ayarlıysa webhook) → hook kullanıcıya tek satır `systemMessage` ile döner (bekleme sırasında çalışma ağacı değiştiyse ya da limit oturumun alt ajanlarını yarıda kestiyse Claude'a da kısa bir `additionalContext` notu) → sonraki model çağrısı sıfırlanmış pencerede yapılır. Ek token: bekleme bağlama metin eklemez; ama istem önbelleğinin (prompt cache) ömründen uzun sürerse sonraki ilk çağrı bütün bağlamı önbelleğe yeniden yazar (bağlam boyu kadar cache write; `noctis report`'ta görünür).

**B. Aynı durumda kullanıcı Esc'ye bastı / VS Code kapandı.** Hook öldü; bekleme kaydı kaldı. Kullanıcı aynı oturumda yazmaya devam ederse ilk hook "yarım kalmış hook içi bekleme" görür ve izleyiciyi iptal eder. Kimse devam etmezse izleyici görev reset+270 s'de çalışır: transcript reset'ten sonra değişmemişse yeni pencerede `--resume` ile devam eder.

**C. Haftalık %95, 2 gün kaldı.** PostToolBatch → checkpoint → `continue:false` + stopReason (≈45 token, konuşmada kalır) → görev reset+90 s. UserPromptSubmit gelirse prompt bloklanır, metni `queuedPrompt` olarak saklanır ve devamda gönderilir.

**D. Fable %97 (OAuth verisi).** UserPromptSubmit → settings.model=opus, `modelSwitched` kaydı, prompt bloklanır ve "/model opus sonra tekrar gönder" denir (token 0; PostModelSwitch hook'u yeni modeli kaydeder, tekrar gönderilen prompt geçer). PostToolBatch'te yakalanırsa checkpoint + dur + 20 s sonra runner Opus ile yeni pencerede `--resume`. Yeni pencere `NOCTIS_HANDOFF=<sid>` ortam değişkeniyle tanınır; eski pencere kilitlenir. Fable kovası sıfırlanınca (`fableResetsAt` geçince) varsayılan `fable`'a döner.

**E. Gerçek 429.** StopFailure → OAuth'tan taze veri → suçlu pencere belirlenir: hata mesajı bir pencereyi adlandırıyorsa o, yoksa sunucu tavanına yakın (≥%90) pencere; oturum Fable'daysa ve Fable kovası ≥%90 ise (ya da mesaj Fable diyorsa) Fable kapasitesi → Opus'a geçiş + 120 s sonra Opus ile devam. Suçlu yoksa geçici hata: aynı modelde 10/20/30/45/60 dk sonra tekrar dener (`wait.retryMinutes`), art arda altıncı hatada bildirimle bırakır. Fable dışında açık oturum yeniden başlatılmadan yerinde uyandırılır (`wake.sameSession`, en fazla 330 dk sonrası için; bilinen bir pencereden ya da aşırı yükten sonra limitler önce yeniden okunur), runner 300 s sonra uyandırmanın tutup tutmadığına bakar. Dahili otomatik devam tetiklenirse (`quota_auto_resume_fired`) runner iptal; `stale`/`disabled` gelirse toast + runner planlanan saatte devam ettirir. Kullanım verisi hiç yoksa runner 10, 10, 20, 30 ve 45 dk arayla veriye bakar, beşinci bakışta bildirimle vazgeçer (sonsuz pencere döngüsü yok).

## 4. Token maliyeti

| Yol | Model bağlamına giren metin |
| --- | --- |
| statusLine | 0 (Claude'a gösterilmez) |
| Hook, eşik altı (her prompt ve araç turu) | 0 (çıktı yok) |
| Hook içi bekleme | Metin 0 (kullanıcıya tek satır `systemMessage`; çalışma ağacı değiştiyse ya da alt ajanlar yarıda kesildiyse kısa bir `additionalContext` notu). Bekleme istem önbelleğinin ömrünü aşarsa sonraki ilk çağrı bütün bağlamı önbelleğe yeniden yazar (bağlam boyu kadar cache write) |
| Uzun bekleme durdurma mesajı | ~40–60 token (bir kez) |
| Fable geçiş mesajı | ~40 token (bir kez) |
| Devam prompt'u | ~25 token (veya kuyruğa alınan kendi prompt'unuz); güvenilen kuyrukta liste adı ve en fazla 3 sonraki madde eklenir. `--resume` bütün konuşmayı geri yükler: istem önbelleğinin ömrü dolduktan sonraki devamda ilk çağrı bütün bağlamı önbelleğe yeniden yazar |
| SessionStart işaretçisi | ~60 token, yalnızca yetim checkpoint varsa, bir kez |
| Yönlendirme talimatı (additionalContext) | ~70 token, yalnızca araştırma isteklerinde |
| `systemMessage` bildirimleri | 0 (yalnızca kullanıcıya gösterilir) |

Checkpoint model çağrısı yapmadan transcript'ten üretilir; `--resume` tam bağlamı geri getirdiği için ek özetleme tokenı harcanmaz. Uzun beklemenin asıl bedeli önbellektir: hook içinde beklense de `--resume` ile dönülse de istem önbelleğinin ömrü dolduysa ilk çağrı bütün bağlamı önbelleğe yeniden yazar; `noctis report` bunu cache write sütununda gösterir.

**F. Araştırma isteği.** UserPromptSubmit → deterministik sınıflandırıcı (kod sinyali yok + araştırma sinyali/URL var) → Fable'a tek satır talimat + kullanıcıya tek satır bildirim → Fable `noctis:lite` (Opus 5, xhigh) alt-ajanına tek Agent çağrısı yapar → arama/okuma tokenları Opus'ta ve alt-ajanın kendi bağlamında kalır → Fable sonucu aktarır. Fable ana thread'de WebSearch/WebFetch denerse PreToolUse (agent_id yoksa) en fazla 3 kez reddeder; sonraki prompt yönlendirmeyi sıfırlar.

## 5. Hata senaryoları ve alınan önlemler

| # | Senaryo | Önlem |
| --- | --- | --- |
| 1 | usage.json hiç yok (statusLine kurulmamış) | Kural uygulanmaz (fail-open), oturum başına bir kez `systemMessage` uyarısı; `doctor` gösterir |
| 2 | usage.json eski (`-p` çalıştırması, statusLine çalışmıyor) | 20 dk'dan eskiyse OAuth yedeğinden 5s/7g okunur; hangisi tazeyse o kullanılır |
| 3 | Pencere `resets_at` geçmiş ama % hâlâ yüksek görünüyor | `resets_at ≤ now` olan pencere yok sayılır; bekleme sonrası tekrar tetiklenmez |
| 4 | Her iki pencere de eşikte | Daha geç sıfırlanan seçilir |
| 5 | Eşik değeri saçma (0, "abc", 150) | Kural devre dışı, diğerleri çalışır |
| 6 | config.json bozuk JSON | Varsayılanlar kullanılır, bir kez uyarı, `status`/`doctor` gösterir |
| 7 | settings.json bozuk JSON | Asla üzerine yazılmaz (model geçişi log'a düşer); kurulum reddeder |
| 8 | Aynı state.json'a paralel hook yazımı (alt ajan + ana ajan) | `wx` kilit + 3 s bekleme + 15 s bayat kilit temizliği; test: 12 paralel yazım kayıpsız |
| 9 | PC uykuya girdi, hook içi bekleme | 15 s dilimli duvar saati kontrolü; uyanınca kalan süreye göre devam |
| 10 | VS Code beklemede kapandı | İzleyici görev reset+270 s'de yeni pencerede devam ettirir |
| 11 | Kullanıcı Esc ile beklemeyi kesti ve devam etti | Sonraki hook yarım beklemeyi tanır, izleyiciyi iptal eder (hayalet pencere yok) |
| 12 | Runner ve dahili auto-continue aynı anda | `quota_auto_resume_fired` → runner iptal; ayrıca transcript reset'ten sonra değiştiyse runner hiçbir şey yapmaz |
| 13 | Aynı oturum id ile yeni pencere: hook kendi kendini bloklar | `NOCTIS_HANDOFF` ortam işareti; eski pencere kilitli, yeni pencere serbest |
| 14 | Yeni pencere Opus'ta ama eski veride model Fable görünüyor | Runner `modelOverrides[sid]=opus` yazar; ilk prompt Fable kuralına takılmaz |
| 15 | Aynı sid için ikinci bekleme → eski sleeper yetim kalır | `registerWait` önce eski görevi/sleeper'ı iptal eder |
| 16 | Runner'ın başlattığı oturum kendi içinde yeni bekleme planlar, runner biterken kaydı siler | Silme yalnızca kayıt aynı `startedAt`'a sahipse; runner kendi `scheduled` bilgisini başlatmadan önce düşürür (kendini öldürmez) |
| 17 | Görev Zamanlayıcı yok / PowerShell hata | Ayrılmış `noctis sleeper` sürecine (aynı binary) düşer, log'a yazar; `status`'ta yöntem görünür |
| 18 | Görev geçmiş bir saate kuruluyor | En az now+15 s; `StartWhenAvailable` kaçırılan görevi hemen çalıştırır |
| 19 | Laptop pilde | `AllowStartIfOnBatteries`, `DontStopIfGoingOnBatteries`, `WakeToRun` |
| 20 | Runner çalışırken hata fırlattı | `main`'deki `recover` → errors.log'a `fatal:` satırı; yeniden başlatma olmazsa (claude yok, klasör yok, başlatma başarısız) bildirim elle çalıştırılacak komutu verir |
| 21 | Runner tekrar 429 yiyor (veri yok) | Deneme sayacı, 10/20/30/45 dk geri çekilme (`wait.retryMinutes`), beşinci bakışta bildirimle durma |
| 22 | OAuth token yok/expired/401/429 | Geri çekilme: token yoksa ve 401/403'te 30 dk, 429'da ve tanınmayan yanıt biçiminde 10 dk, diğer hatalarda 2 dk; Fable kuralı pasif, token asla log'a yazılmaz |
| 23 | OAuth yanıt şeması değişti | `limits[]` + düz alanlar birlikte denenir; Fable kovası bulunamazsa `note` ile görünür kılınır |
| 24 | Fable eşiğinde `-p` (yönetici) çağrısı | Prompt bloklanır, mesaj çıktıya düşer; yönetici `noctis model` ile Opus seçer |
| 25 | Devam prompt'unda tırnak/`%`/`^`/satır sonu | `sanitizePrompt` + tam cmd meta-karakter tırnaklaması; Windows pencere modu JSON spec + `Start-Process` (kabuk yok) |
| 26 | Kullanıcı adında boşluk/Türkçe karakter | Tüm yollar JSON/argv ile taşınır; statusLine komutu tırnaklı ve ileri eğik çizgili |
| 27 | Oturum id'si tuhaf (`__proto__`, `../x`) | `sessionKey` temizler; dosya adları `safeName` ile |
| 28 | Transcript devasa | Yalnızca son 256 KB okunur, ilk yarım satır atılır |
| 29 | Checkpoint yetim kaldı ama iş çoktan bitti | 7 gün TTL, tüketilince bir daha gösterilmez; başarılı devam/resume tüketir |
| 30 | `guard.js cancel` bekleme yüzünden bloklanıyor | Eklentinin kendi skill prompt'ları hook'tan muaf |
| 31 | Log sınırsız büyüyor | 512 KB'de `.1`'e döndürülür |
| 32 | `auto` izin modu eski sürümde yok | `claude --help` çıktısında doğrulanır, yoksa `acceptEdits` |
| 33 | Kod işi yanlışlıkla ucuz modele gider | Sınıflandırıcı tek yönlü temkinli: herhangi bir kod sinyali (kelime kökü, dosya yolu/uzantısı, sembol) yönlendirmeyi keser; kısa/süreç cümleleri hariç; Fable'a "kod gerekiyorsa normal çalış" kaçışı; alt-ajan `NEEDS_CODE` ile geri verir |
| 34 | Fable talimata uymayıp kendi araştırır | PreToolUse WebSearch/WebFetch reddi (ana thread, 3 deneme), alt-ajan aramaları `agent_id` ile serbest |
| 35 | Yönlendirme reddi sonsuz döngüye girer | Deneme sayacı; 3'ten sonra izin verilir ve WARN yazılır |
| 36 | Ana oturum ve izleyici görev PC uykusundan aynı anda uyanır | Hook kalp atışı (her 15 sn'lik dilimde; 120 sn'den yeniyse canlı) + runner 45 sn bekleyip yeniden okur; kalp atışı tazeyse 5 dk sonra tekrar bakar (en fazla 6 kez) |
| 37 | state.json aylar içinde şişer | Oturum kapanışında (SessionEnd) ve her yazımda 3 günlük TTL ile budama |
| 38 | Hatalar kayboluyor | `errors.log` yalnızca WARN/ERROR; `doctor` ve `status` son kayıtları gösterir |
| 39 | Tek büyük model çağrısı %91'den %100'e sıçratıyor | Ani yükseliş öngörüsü: aynı penceredeki son 30 dk sıçramalarının en büyüğü + mevcut kullanım ≥ %100 ise (kullanım ≥ %60 şartıyla) eşikten önce durur |
| 40 | Durum çubuğu verisi hızlı araç döngüsünde geride kalır | Eşiğe 8 puan kala her hook'ta (60 sn'de bir) OAuth'tan doğrudan doğrulama |
| 41 | Sert duruş yarım işi bozar | Eşiğe 6 puan kala Claude'a tek seferlik "adımı temiz bitir" bağlamı |
| 42 | Kullanıcı bilgisayardan uzakta, toast görmüyor | İsteğe bağlı `alarm.webhookUrl` (https) ile JSON POST |
| 43 | checkpoints/ klasörü şişer, resume-output.log sınırsız büyür | TTL sonunda dosya silinir; log 512 KB'de döner |
| 44 | Hook başlatma gecikmesi araç döngüsünü yavaşlatır | Ağır modüller (https/http/child_process) tembel yüklenir: ~47 ms → ~32 ms; Notification/PostModelSwitch/SessionEnd/StopFailure hook'ları `async` |
| 45 | Aynı hesapta iki pencere, iki statusLine süreci usage.json'ı ezer (oturum modeli / sıçrama geçmişi kaybolur) | `usage.lock` ile kilitli read-modify-write (test: 12 paralel yazım, hepsi korunur) |
| 46 | PC saati kaymış: pencere yerelde açık görünür, sunucuda kapanmış (veya tersi) | OAuth yanıtındaki `Date` ile kayma ölçülür (30 sn–24 sa arası), tüm `resets_at` değerleri yerel saate çevrilir; `status` gösterir |
| 47 | Node güncellendi, hooks.json'daki mutlak yol öldü → hook'lar sessizce çalışmaz | statusLine (bare `node` ile çalışır) 10 dk'da bir yolu doğrular, yoksa `process.execPath` ile onarır ve WARN yazar |
| 48 | Kablolama sonradan bozuldu (statusLine başka eklentiyle değişti, config bozuk, token yok) | Oturum açılışında öz-denetim; günde en fazla bir kez tek satır `systemMessage` |
| 49 | 429 sonrası suçlu pencere eşik çok yüksek ayarlandığı için bulunamıyor | Suçlu tespitinde taban %90 |
| 50 | Bir sonraki çağrı görülen en büyük sıçramadan büyük | Öngörüde %20 emniyet çarpanı (`used + burst×1.2 ≥ 100`) |
| 51 | "Bu konuşmayı özetle" alt-ajana gider, alt-ajan konuşmayı bilmez | Özet kelimesi yalnızca URL veya ≥1200 karakter yapıştırılmış metinle yönlendirir |
| 52 | Kodlama sırasında "şunu araştır" aslında kod tabanı incelemesi | Transcript'in son 64 KB'ında 45 dk içinde Edit/Write/Bash varsa genel araştırma kelimeleri yönlendirmez; web sinyalleri ve URL yine yönlendirir |
| 53 | Alt-ajan projenin CLAUDE.md'sini yükleyip token yakar | `omitClaudeMd: true` |
| 54 | Kenar bandında sabit 60 sn yoklama ya fazla ya az | Yakım hızına göre 30/60/120 sn |
| 55 | Eşik değeri yanlış girildi (0, "abc", 150) → kural sessizce kapalı | Öz-denetim günde bir kez uyarır |
| 56 | Otomatik sıkıştırma (devasa tek çağrı) eşiğin hemen altında limiti aşırır; hook araya giremez | Oturum bağlamı ≥%85 ve kullanım eşiğe ≤6 puan kalmışsa sıkıştırma gelmeden durdur (`compaction` nedeni) |
| 57 | Sıkıştırma sonrası checkpoint "son istek" olarak özet metnini alır | `isCompactSummary`/önek tespiti; özet ayrı bölümde, gerçek istek korunur |
| 58 | Esc + `/clear` sonrası eski oturumun izleyicisi saatler sonra pencere açar | SessionStart(clear): aynı klasörde son 10 dk'da güncellenen ve hook içi beklemesi olan oturum serbest bırakılır; park edilmiş uzun beklemeler dokunulmaz |
| 59 | `/clear` sonrası canlı model bilinmez, Fable kuralı ilk prompt'ta yanlış çalışabilir | Önceki oturumun canlı modeli yeni kimliğe taşınır |
| 60 | Geçici API 429'u Fable kapasitesi sanılıp varsayılan model bir hafta Opus'a takılır | Fable şüphesi yalnızca OAuth Fable kovası ≥%90 ise; aksi halde aynı modelde 10/20/30/45/60 dk sonra tekrar dene, art arda altıncı hatada bildir |
| 61 | StopFailure `async` iken `-p` süreci çıkınca planlama yarım kalabilir | StopFailure senkron; yalnızca gözlemsel hook'lar async |
| 62 | hooks.json salt okunur konumda; öz-onarım yazamayınca durum çubuğu kırılır | Öz-onarım hatası yutulur ve WARN yazılır |
| 63 | state.json yarım yazılmış/bozuk (elektrik kesintisi, disk dolu) → bekleyen işler kaybolur | Her yazım öncesi `.bak`; okunamazsa yedekten toparlanır |
| 65 | Ana oturum %91'de alt-ajan(lar) başlatır; alt-ajanın ilk model çağrısı kapısızdır | `PreToolUse` Agent/Task kapısı: eşikteyse bekleme/devir, Fable eşiğindeyse geçiş mesajıyla ret |
| 67 | Sabit uyarı bandı hızlı yakımda geç kalır | Bant `max(6, 2×sıçrama)`, en fazla 15 puan |
| 68 | Yüzlerce işlik kuyrukta sıkıştırma/`/clear` sonrası "neredeydik" kaybolur | Dosya tabanlı kuyruk: açılış ve her sıkıştırmada talimat, checkpoint'te açık maddeler, devam prompt'unda liste işareti; görev olayları (TaskCreated/Completed) kaydedilir |
| 69 | Kodsuz işlerin çıktısı (metin) Fable'ın çıktı token'ıyla dosyaya yazılıyordu | lite ajanı .md/.txt yazabilir; kod uzantıları PreToolUse ile yasak (ajan türü kontrolüyle yalnızca lite'a uygulanır) |
| 70 | Hook'lar sessizce çalışmayı bırakır (şema değişikliği, bozuk kayıt) | Hook nabzı (10 dk) + durum çubuğunda `⚠ hook yok` + errors.log (günde bir) |
| 71 | Claude Code hook zaman aşımına tavan getirir; hook içi bekleme yarıda kesilir | Kesilme süreleri öğrenilir, daha uzun beklemeler runner'a gider |
| 72 | `-p` işçi oturumu için pencere açılır | Durum çubuğu görmeyen oturum headless çıkarımı |
| 73 | Görev Zamanlayıcı mutlak node yoluna bağlı; Node taşınınca bekleyen görevler ölür | `runner.cmd`: PATH'teki node önce, mutlak yol yedek |
| 74 | Haftalık eşiğe sıfırlanmadan önce çarpılacağı görülmez | Yakım eğiminden ETA, yalnızca anlamlıysa gösterilir |
| 75 | API kesintisi + durum çubuğu karartması eşikte körlük yaratır (hard mod'da Fable %99'a çıktı, kimse görmedi) | Kör-nokta koruması: 2 dk bayat + API hatası + kenar → 5×60 sn yoklama, hâlâ yoksa dur; Fable kenarında 60 sn yoklama; zorunlu tazeleme backoff'u aşar |
| 76 | 15 dk sabit backoff geçici 500'lerde uzun körlük | Kademeli: 500/timeout/bozuk gövde 2 dk, 429 10 dk, 401/403 30 dk |
| 77 | usage.json bozulunca sıçrama geçmişi ve oturum modelleri kaybolur | `.bak` + kurtarma |
| 78 | Alt-ajan başlatırken Fable eşiği: yalnızca mesaj | Checkpoint + Opus ile yeniden başlatma (batch yolu) |
| 79 | Devralan runner ölürse handoff kilidi kalıcı; yedekten kurtarma eski kilidi diriltebilir | Handoff kaydı runner pid taşır, pid ölüyse 60 sn sonra düşer; 48 saati geçmiş yetim bekleme temizlenir |
| 82 | Yanlış yönlendirme (lite `NEEDS_CODE`) aynı kelimeyle tekrar eder | PostToolUse(Agent) sonucu izler; sinyal başına hata/başarı sayacı, 2 hata → kapalı, başarı > hata → açık |
| 83 | Saatler sonra dönen oturum hangi dosyaların yarım kaldığını bilmez | Checkpoint'e `git status --short` |
| 84 | Fable'ın haftadan önce biteceği görülmez | fable.json geçmişinden ETA, durum çubuğunda |
| 85 | Claude madde arasında "diğer işe geçiyorum" deyip turu bitirir, kuyruk durur | Stop hook: açık madde varsa devam zorlanır; ilerlemesiz 4 zorlama ya da 200 zorlama → dur + bildirim (aynı dosya için bir kez), vazgeçme iki sayacı da sıfırlar; oturum başına yerel takvim gününde 600 tavan (`queue.maxContinuesPerDay`; vazgeçme sıfırlamaz, gece yarısı sıfırlanır); limitteyse zorlama yok |
| 86 | (kaldırıldı) Temiz oturumla yeniden başlatma fikri | Kullanıcı kararı: iş aynı oturumda kalır, sıkıştırma Claude Code'a bırakılır; eklenti bağlam yüzünden asla oturum kapatmaz |
| 91 | Durum çubuğu susmuş + API kesik: son bilinen düşük değerle körlemesine devam (hard modda %100'e vurdu) | Bayat veri öngörüsü: `used + eğim × bayatlık` eşiği aşarsa dur; durum çubuğu görmemiş oturumda 60 sn'de bayat |
| 87 | Kullanıcı autocompact'ı 60'a çekmiş, %85 kapısı hiç çalışmaz | `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` okunur, kapı override−5 |
| 88 | Yeni pencereler Remote Control'de görünmez | `resume.remoteControl` → `--remote-control`, `resume.extraArgs` |
| 89 | Kurulum sonrası "gerçekten çalışıyor mu" bilinmez | `selftest`: gerçek görev + fire, toast, OAuth, sürüm aralığı, hook nabzı |
| 90 | Token tasarrufu tahmin | `report`: transcript `usage` alanlarından gerçek ölçüm |
| 93 | Binary `<root>/bin/noctis`'de iken plugin kökü bir seviye yukarı çözülüyordu; lab env ile maskeliyordu | Kök tespiti: `NOCTIS_PLUGIN_ROOT` → `CLAUDE_PLUGIN_ROOT` → yukarı doğru `config.default.json + hooks/hooks.json` arama; lab artık env'siz de doğruluyor |
| 94 | SIGKILL edilen hook `fable.lock` bırakıyor, 15 sn kör pencere | Kilitler sahibinin pid'ini taşır; ölü sahip anında temizlenir (`lockAbandoned`), her nabızda süpürülür |
| 95 | Soak `hook-kill` kaosu Go'nun hızı yüzünden `close` olayını kaçırıp sonsuza kadar bekliyordu | Test hatası: dinleyici öldürmeden önce bağlanır |
| 96 | Kendi kendini üzerine yazma (`setup` çalışan binary'yi yerleştirmeye çalışınca ETXTBSY) | Çalışan binary hedefse veya içerik aynıysa atlanır |
| 97 | Marketplace bootstrap hook'u `sh ensure-binary.sh` çağırıyordu: Git Bash olmayan Windows'ta her oturum başında hata; kabuk biçimli komutlar PowerShell'de `& ` ister, bash'te istemez — iki kabuğa uyan tek komut yok | Kabuk tamamen kaldırıldı: tüm hook'lar exec biçimi, depo `bin/noctis.exe` (windows-amd64) + `bin/noctis` POSIX başlatıcısı gönderir (libuv `bin/noctis` → `.exe` çözer), bootstrap `noctis ensure` binary içinde; Windows'ta çalışan exe yeniden adlandırılıp (`.old`) değiştirilir, geçici dosya pid'li + rename; skill'ler binary'yi doğrudan çağırır; lab senaryosu eklendi |
| 98 | Kuyruk bittiğinde oturum sessizce duruyordu; gece kuyruğu süren kullanıcı sabaha kadar bitip bitmediğini bilemiyordu | Stop hook, kuyruğu bu oturumda sürdüyse (stopGuard) tek satır `✔ Kuyruk bitti` + masaüstü bildirimi/webhook, karar günlüğüne `queue finished`; sonraki Stop'lar sessiz |
| 99 | Aynı bekleme için iki runner (eski sleeper + elle `noctis resume`, ya da iki zamanlayıcı) aynı anda ateşlenince iki `claude --resume` açılıyordu (hard soak `double-resume` kaosunda yakalandı) | Oturum state kilidi altında sahiplenilir: `handedOff[sid]` canlı bir pid taşıyorsa ikinci runner başlatmaz (`skip-launch` günlüğü) |
| 100 | Taze kurulumda ilk `SessionStart` yerel veri olmadığı için %99'daki kullanıcıya hiçbir şey söylemiyordu | Yerel veri yoksa/bayatsa tek OAuth okuması (5 sn zaman aşımı, geri çekilmeye saygılı), ardından `session.alreadyOver` bildirimi pencere+sıfırlanma başına bir kez |
| 101 | Kuyruk vazgeçmesi (`stuck`) stopGuard kaydını siliyordu; sonra kuyruk bitince "bu oturum sürdü mü" bilgisi kaybolduğundan `✔ Kuyruk bitti` bildirimi gelmiyordu | Vazgeçmede sayaçlar sıfırlanır ama kayıt kalır (`cycles` artar); aynı dosya için `stuck` uyarısı tekrarlanmaz; bitiş bildirimi korunur |
| 102 | Aynı pencere için ikinci bir prompt beklemesi "join-wait" kısayoluna takılıp `queuedPrompt`'u güncellemiyor, izleyiciyi değiştirmiyordu | Kısayol yalnızca batch/stop türleri için; prompt türü her zaman kuyruğa alınır ve izleyici yenilenir |
| 103 | 529/5xx geri çekilmesi sonrası yeniden başlatma prompt'u genel "devam et" metniydi; Claude'un kesintinin ne olduğunu bilmesi gerekir | Devam prompt'u `overload.wakeMessage` ile başlar (hata türü, deneme sayısı, beklenen süre); TR kataloğundaki İngilizce kalmış iki yönerge çevrildi |
| 104 | Ölü/askıda kalmış lock: canlı sahipli kilit 120 sn, pid'siz kilit `lockStaleMs` sonra; silinemeyen kilitte döngü | `lockAbandoned` üç durumlu; `withFileLock` silinemeyen bayat kilitte dönmez, uyarı yazar |
| 105 | Kör-nokta yoklaması hiç veri alınamamış (token yok / 40x) hesaplarda da 5×60 sn bekletiyordu | Yoklama yalnızca en az bir kez başarılı okuma varsa ve hata kalıcı değilse |
| 106 | Zamanlayıcı kurulumu kilitsizdi: paralel hook'lar aynı sid için iki sleeper açabiliyordu | `schedule.lock` altında `scheduleRunnerLocked`; sleeper pid 0 ise manuel yöntem |
| 107 | Tamamlama vaadi transcript'in herhangi bir yerinde aranıyordu (eski turdaki `<promise>` döngüyü bitiriyordu) | Yalnızca son asistan mesajının ham metninde (`lastAssistantRaw`) aranır |
| 108 | Aşırı yük bölümü hiç sıfırlanmıyordu: günler sonra gelen tek 529 "25. deneme" sayılıp anında vazgeçiyordu | Bölüm sınırı boşluk tabanlı (`2×maxSeconds+60`); başarılı prompt/batch/stop bölümü temizler; 24 sa sonra budanır |
| 109 | Hook tavanı öğrenmesi tek kesintide tetikleniyor, tamamlanan bekleme tavanı temizlemiyordu | İki ardışık kesinti gerekir; tavanı aşan bir bekleme tamamlanınca tavan silinir; `noctis on` sıfırlar |
| 110 | Alt-ajan kapısı ad alanlı ajan türünü (`noctis:lite`) çıplak adla eşleştiremiyordu | `agentWritePolicy` çıplak ve ad alanlı türü kabul eder |
| 111 | CJK/TR kataloglarında `queue.blockedNotify` argüman sırası (%d, %s) cümle yapısına uymuyordu | Dizinli fiiller `%[2]s … %[1]d`; katalog eşlik taraması |
| 112 | `state.json` yazımı: geçici dosya rename'i Windows'ta AV kilidiyle düşebiliyor, kodlama hatası yarım dosya bırakabiliyordu | `writeJSONAtomic` rename'i 5 kez dener, encode hatasında yazmayı reddeder; `marshalPretty` hata döner |
| 113 | Hook/statusline komutları stderr'e yazınca Claude Code bunları kullanıcıya gösteriyordu | `logLevel` hook/statusline modunda stderr'e asla yazmaz (yalnızca dosya) |
| 114 | `SessionStart source=resume` erken dönüyor, kuyruk yönergesi ve %99 uyarısı atlanıyordu; checkpoint bağlamı `compact`'ta da enjekte ediliyordu | Erken dönüş kaldırıldı; checkpoint bağlamı yalnızca `startup|clear` |
| 115 | `noctis on` öğrenilmiş hook tavanını ve kesilen bekleme sayacını taşıyordu | `runOn` ikisini de sıfırlar |
| 116 | 429'un suçlusu yalnızca yüzdelerden tahmin ediliyordu; Claude Code'un `error_message` alanı ("weekly limit", "5-hour", Fable) okunmuyordu | `limitHint`: mesaj pencereyi adlandırıyorsa o kazanır (kova verisi varsa); günlüğe `hint` + mesaj yazılır |
| 117 | Fable → Opus geri dönüşü yalnızca hook içinde çalışıyordu; sıfırlanmadan sonra açılan yeni oturum Opus'ta başlıyordu (soak sabah değişmezi yakaladı) | `SessionStart` (startup/resume) geri dönüşü uygular ve bildirir |
| 118 | Kenar tazeleme aralığı yalnızca sıçrama büyüklüğüne bakıyordu (30/60/120 sn); eşiğe 1.5 puan kala 60 sn'lik önbellek + atlanan durum çubuğu güncellemesi + 7 puanlık tek çağrı = %97.5'te izin verilen çağrı (14 günlük soak, seed 3) | Aralık eşiğe uzaklığa da bağlı: ≤2 puan → 15 sn, ≤4 puan → 30 sn (`nearEdgePollClose`) |
| 119 | Duvarda park etmiş oturumun penceresi kapatılınca (`SessionEnd`) workflow kaydı siliniyor, 3 günlük TTL uzun beklemelerde de budanıyordu; runner'ın yeniden başlatma prompt'u workflow'u anmıyor, Claude sıfırdan başlıyordu (hard soak yakaladı) | Bekleyen `waits[sid]` varken `SessionEnd` ve budama workflow kaydına dokunmaz |
| 92 | Node başlatma hook başına ~25 ms yiyor, Node kurulu olmayan makinede plugin ölü | Motor Go'ya taşındı (`go/cmd/noctis`, stdlib-only, 6 platform binary'si repo'da); lab 179/179 ve kaos soak birebir; p50 41 → 7 ms; kurulum PowerShell/sh + binary içi `install` |
| 64 | 429 suçlusu kullanıcı eşiğine göre aranıyordu; hafta %87'deyken gelen geçici 429 "haftalık kapasite" sanılıp iş bir hafta beklemeye alınabilirdi (soak'ta yakalandı) | Suçlu yalnızca sunucu tavanına yakın (≥%90) pencere; aksi halde geçici hata: 10/20/30/45/60 dk merdiveniyle tekrar |

## 5b. Benzer projelerden alınan fikirler

- **heavy-usage** (statusLine → JSON → UserPromptSubmit, iki fazlı uyarı/wind-down): uyarı bandı buradan; fark: bizde durdurma her model çağrısından önce (PostToolBatch) ve hook içi bekleme ile bağlam korunuyor.
- **usage-guard** (PreToolUse `continue:false`, snooze, Slack/webhook bildirimleri): webhook alarmı buradan; snooze bizde `off <dk>`.
- **claude-code-cockpit** (StopFailure + `asyncRewake` + exit 2 ile aynı oturumu uyandırma): belgelenmemiş davranışa dayandığı ve dahili otomatik devamla çift devam riski taşıdığı için önce alınmadı; v5'te runner yedeğiyle alındı (5f madde 8, `wake.sameSession`): bilinen bir pencereden ya da aşırı yükten sonra uyandırmadan önce limitler yeniden okunur, oturum hâlâ duraklama noktasındaysa runner'a bırakılır, uyandırma tutmazsa runner 300 s sonra yeniden başlatır. Veri yoksa açık oturum 10 dk sonra uyandırılır; yeniden başlatma 10, 10, 20, 30, 45 dk arayla veriye bakar ve beşinci bakışta vazgeçer.
- **claude-auto-retry** (tmux'a "continue" yazma): terminale bağımlı; bizde `--resume` ile yeni süreç.
- **Claude-Code-Usage-Monitor** (burn-rate ve bitiş tahmini): ani yükseliş öngörüsü buradan esinlenildi, ancak sadece resmi `rate_limits` deltalarıyla.

## 5c. Soak (dayanıklılık) testi

`tests/soak.js`: sahte saat (`NOCTIS_TIME_OFFSET`), test-zamanlayıcı (`NOCTIS_NO_SCHEDULE`), gerçekçi tüketim modeli, iki hesap, rastgele kaos (clear / sıkıştırma / geçici 429 / paralel hook'lar / debounce). 7–14 gün × 2 bağımsız hesap = ~800–1500 hook; 0 aşım, 0 anomali (seed 42 ve 7). Yakaladığı gerçek hata: #64.

## 5d. Hard mod (kaos) soak

`tests/soak.js --hard 1`: maraton oturumlar (günde 28 tur, 1–4 araç turu, %20 alt-ajan başlatma), 12 çeşit kaos enjeksiyonu, iki bağımsız hesap. Bulduğu gerçek hatalar: #75–#79. Sonuç (3 seed × 5 gün): 0 aşım, 0 anomali, en yüksek izin verilen 5s %91.7 (eşik 92).

## 5e. Go portu

Spec = donmuş `guard.js`; port modül modül (depo/kilit/state → kullanım/OAuth/değerlendirme → yönlendirici → hook'lar → statusline → runner → komutlar → kurulum). Kapı: aynı lab ve soak, `node` yerine binary'yi çağırır. RE2 lookbehind desteklemediği için kelime sınırları tüketen Unicode sınıflarla yazıldı; 31 prompt'ta Node ile birebir sonuç. Sonuç: 179/179, hard soak 0 aşım, hook p50 7 ms / p95 9 ms.

## 5f. v5 — genel kullanıcı sürümü (19 madde)

1 i18n (`messages.go`, en/tr, `NOCTIS_LANG`/`LANG`) · 2 marketplace + `setup` + preset + plan penceresi tespiti · 3 Keychain + launchd/systemd · 4 CI matrisi · 5 alt-ajan model sabitleme (`updatedInput`) · 6 çoklu oturum maksimumu (yalnızca farklı oturumlar arası) · 7 tek-uçuş API kilidi · 8 aynı oturumda uyanma (async + exit 2, runner yedek) · 9 digest ajanı · 10 tempo göstergesi + günlük bütçe · 11 completionPromise + pause/resume · 12 statusline modları · 13 genel kapsamlı-model kuralı · 14 report json/html + keptOffPrimary · 15 webhook retry/devre kesici/ön ayarlar · 16 gözlem modu · 17 `why` karar günlüğü · 18 izin modu mirası · 19 başlatma anlık görüntüsü: Go'da gereksiz (7 ms), yapılmadı.

Sonuç (5.0.0 final, altı platform binary'si yeniden derlendi, `bin/SHA256SUMS`): lab 248/248; hard soak 3 gün 0 aşım 0 anomali, hook p50 9 ms / p95 14 ms; normal soak 7 gün 0 aşım 0 anomali, p50 8 ms. `gofmt`/`go vet` temiz, ölü fonksiyon/katalog anahtarı/config anahtarı taraması temiz (en/tr katalog 182/182 anahtar, biçim fiilleri eş).

## 5g. v5.1 — herkes için (rol profili, izin, dil, workflow, özensiz kuyruk)

Kullanıcı isteği: en bilinçsiz kullanıcı bile sorun yaşamamalı. Yapılanlar: (1) `setup` rol profili sorar (`roles.go`: synex/balanced/economy/custom; `models.*`, `router.subagentModels`, ajan frontmatter'ı projeksiyon; `noctis ensure` marketplace güncellemesinde ajanları hizalar). (2) `permissions.defaultMode` en geniş güvenli mod (`auto` yoksa `acceptEdits`), `managedPermissionMode` ile geri alınabilir. (3) 14 dil: `lang.go` deterministik tespit (alfabe + sık kelimeler), oturum başına `sessionLocale`, `locale: auto` iken bildirim/statusline/toast o dilde; Claude'a giden `[noctis]` yönergeleri de katalogdan. (4) Workflow farkındalığı: `workflow.go` — dağıtık iş tespiti (EN/TR desenler), öneri rol modelleriyle, `PreToolUse Workflow` kapısı (uyarı bandı/eşik/Fable bitti → deny), her başlatma `workflows[sid]`'de; checkpoint + devam prompt'u + uyanma mesajı "aynı script'i yeniden başlat, sıfırdan başlama". (5) Özensiz kuyruk: `queueItemPattern` toleranslı kutular (`-[ ]`, `* [ ]`, `1. [ ]`, `[]`, `TODO:`), devam satırları tek maddeye katlanır, kutusuz liste "önce kutulu yaz" yönergesi, `(P0)`–`(P9)`, `#etiket`, `(after …)`, olmayan referans takılmaz, 1 MB tavan. (6) %99 kurulum: `session.alreadyOver` (bir kez/pencere), ilk prompt park, `noctis check` 11; Fable kovası/haftalık limit olmayan planlarda kurallar sessiz. (7) 529/5xx geri çekilme, çalışma ağacı koruması, git snapshot, GitHub issue import/close, maliyet raporu, `noctis check`. (8) Bug taraması: 19 bulgu (#99–#119) düzeltildi. Lab 363/363; hard soak 20 kaos çeşidi (529 fırtınası + bütçe tükenmesi, beklerken düzenleme, P0/bloklu madde, dil değişimi, workflow, %99 taze kurulum, çift runner) 7 gün 0 aşım 0 anomali; normal soak 14 gün 0 aşım 0 anomali.

## 5h. v5.2 — beş araç, tek motor

Kullanıcı isteği: diğer yapay zekâlarla da çalışsın, terminalde numarayla seçilsin, güncelleme herkeste otomatik olsun, en basit kullanıcı da anlasın. Araştırma (resmî dokümanlar, Eylül 2026): tam port Codex CLI (Claude'a izomorf hook'lar, `app-server` limitleri, `exec resume`) ve Antigravity CLI (statusline `quota`, `Stop` continue, `--conversation -p`); kısmi Droid ve Copilot (kuyruk + checkpoint + retry; kullanım betiklere kapalı); yok: Gemini CLI (kapandı), Kiro (AgentStop bloklayamaz), Cursor/OpenCode/Amp/Qwen (limit yok), Roo (arşiv), Cline/Goose/Crush. Uygulama: `host.go` — `hostSpec` yetenek bayrakları (limits/statusline/modelSwitch/wake/agents/stopFailure), `normalizeHookInput` (Codex geçiş; Antigravity camelCase + PreInvocation/PostInvocation→PostToolBatch, Stop error→StopFailure; Copilot camelCase, agentStop/errorOccurred), `translateOutput` (Antigravity `decision:continue|deny|allow`, `terminationBehavior:terminate`, `injectSteps`; Copilot düz alanlar), `hostLaunchArgs`, `fetchCodexRateLimits` (JSON-RPC stdio, `windowDurationMins` ile sınıflandırma), `antigravityRateLimits`, hook dosyası yazıcıları (Codex `statusMessage` işareti, Droid olay anahtarlı, Antigravity ad anahtarlı + statusLine, Copilot exec-form tek dosya). `emit` tek noktadan çevirir; handler'lar Claude biçimini bilir. Güncelleme: setup `claude plugin marketplace update <m> --auto-update`; `checkForUpdate` günde bir, 3 sn. Lab 426/426 (sahte codex/agy/droid/copilot; otomatik kuyruk, yeniden başlatma banner'ı, yan yana notları).

| # | Sorun | Çözüm |
|---|---|---|
| 120 | Kaldırma `model` ayarını bırakıyordu; README "geri alır" diyordu | `managedModel {previous, set}`; kaldırma önceki değeri geri yükler ya da anahtarı siler |
| 121 | `TODO.md` varsayılan kuyruk dosyasıydı; not dosyası olan kullanıcı sormadan yürütülen bir kuyruk buluyordu | Varsayılan `TASKS.md, tasks.md, .claude/TASKS.md, docs/TASKS.md`; ilk oturumda `☰ Kuyruk modu` bildirimi (7 günde bir) |
| 122 | TR kataloğunda Claude'a giden iki `[noctis]` yönergesi Türkçe, diğerleri İngilizce; docs "aynı dilde" diyordu | Yönergeler her katalogda İngilizce; docs düzeltildi |
| 123 | Docs launchd/systemd'nin PC'yi uyandırdığını ima ediyordu | Yalnızca Windows uyandırır; README/TR/timeline düzeltildi |
| 124 | `install.ps1` `$Host`/`$Profile` otomatik değişkenleriyle çakışacaktı | `-Tool` / `-Roles` |
| 125 | Kullanım verisi olmayan host'larda (Droid/Copilot) "kullanım verisi yok" bildirimi ve resume'da veri bekleme | `hostSpec.limits` ile kapı: bildirim yok, retry doğrudan başlatılır |
| 126 | Kuyruk yalnızca `TASKS.md` ile çalışıyordu; uzun bir prompt aslında bir görev listesi ama kimse dosya açmaz | `autoqueue.go`: deterministik tespit (≥240 karakter; ≥3 liste maddesi, ya da iş fiiliyle başlayan ≥4 satır, ya da sıra sözcükleriyle bağlı ≥4 böyle cümle; soru, yapıştırılmış log/"steps to reproduce" ve betimleyici bağlam satırları hariç; `[x]` işaretli maddeler atlanır), checklist `<hesap>/noctis/queues/<sid>.md`, `autoQueues[sid]` durumu, `☰ N adımlık iş algılandı` bir kez; proje kuyruk dosyası önceliklidir; kısa/spesifik iş ve sorular dokunulmaz; elle prompt → kuyruk kapanır, dosya silinir |
| 127 | Marketplace otomatik güncellemesi yeni sürümü indirdiğinde kimse haber vermiyordu; eski sürüm çalışmaya devam ediyordu | `newerCachedVersion` önbellekteki daha yeni sürümü görür; `restartNotice` bir kez/sürüm: `⬆ … indirildi — /reload-plugins ya da aracı yeniden başlat` + masaüstü bildirimi (`update.downloadedNotify`) |
| 128 | Diğer eklentilerle çakışma görünmezdi (aynı olaya ikinci Stop hook'u, ikinci auto-resume aracı) | `coexistenceNotes`: `noctis doctor` aynı olaylardaki yabancı hook sayısını ve komşu eklentileri (ralph/loop/resume/statusline/usage/snooze) `ℹ` ile listeler; hiçbir şey değiştirilmez; README "Diğer eklentilerle yan yana" |
| 129 | `synex` profili yeterince agresif değildi; isim artık ürünle uyuşmuyordu | Profil `noctis` (synex takma ad): kod/planlama Fable · max, araştırma Opus 5 · xhigh, özet/keşif Haiku · high, yedek Opus · max; `agents/lite.md` xhigh, `agents/digest.md` high |
| 130 | Ürün yalnızca Claude'a hitap eden bir ada sahipti | Tam yeniden adlandırma: Noctis (`synex1437/noctis`, ikili `noctis`, `NOCTIS_*` ortam değişkenleri, `/noctis:setup`); banner/sosyal önizleme/tüm SVG'ler ve docs güncellendi |
| 131 | Runner'ın devam prompt'u UserPromptSubmit'e insan prompt'u gibi giriyor, açık otomatik kuyruğu siliyor ya da (workspace notu eklenince) çöp maddelerle yeniden yazıyordu; oturum dili de bu İngilizce prompt'tan öğreniliyordu | `runResume` prompt özetini `resumePrompts[sid]` olarak kaydeder; `pluginComposedPrompt` (özet eşleşmesi ya da `resume.prompt` / `overload.wakeMessage` öneki) → dil öğrenilmez, kuyruk ne açılır ne kapanır |
| 132 | Otomatik kuyruk yanlış pozitifler: soru listesi, log yapıştırılmış hata raporu, betimleyici bağlam satırları "iş" sayılıyordu; `[x] …` maddesi `x] …` olarak açılıyordu | Birim başına soru filtresi, log/"steps to reproduce" işaretleri, satır ve paragraf yolunda iş fiili şartı (`imperativeWords`, TR'de cümle sonu) + sıra sözcükleri (paragraf), betimleyici başlangıç sözcükleri; `checkboxPrefix` işaretli maddeleri atlar; `autoqueue_test.go` |
| 133 | `update.available` bildirimi 3 argümanla 4 yer tutuculu biçime basılıyordu (`%!s(MISSING)`) | `pluginName` eklendi; tüm `T()` çağrıları için arity taraması |
| 134 | `runWithTimeout` torun süreç stdout'u tutunca zaman aşımına uymuyordu (daemonlaşan statusline zinciri, Windows `cmd.exe` sarmalayıcıları) | `cmd.WaitDelay = 500 ms`; `ErrWaitDelay` çıktıyla birlikte başarı sayılır |
| 135 | Hook/config/settings yazımı başarısız olsa da setup "tamam" diyordu (yalnızca errors.log'a düşüyordu) | `wireHostHooks` hata döndürür; `writeFailures` sayacı → `install.incomplete` ile çıkış kodu 1; Antigravity statusLine bağlanamayınca satırda söylenir |
| 136 | `--host X --config-dir Y` ile doctor/log'lar `Y` yerine host'un varsayılan klasörüne gidiyordu | `pathsFor` dizini `account` bayrağı olarak verir (initPaths'te önceliklidir) |
| 137 | Windows'ta `where codex` önce uzantısız POSIX shim'i döndürüyor, `exec` başarısız oluyordu (limit koruması kör, devam başlatılamıyor) | `locateExecutable`: `.exe/.cmd` tercih edilir; `claude` ile aynı yol |
| 138 | Zamanlanmış runner/sleeper `--host` almıyordu; config'de `host` yoksa Codex/Droid oturumu Claude yolundan devam ettirilmeye çalışılıyordu | `runnerArgs` her runner/sleeper'a `--host <aktif>` ekler |
| 139 | Codex app-server el sıkışması boru hattı gibi tek seferde gönderiliyordu; boş cevap başarı olarak önbelleğe alınıyordu (10 dk kör) | `waitFor(id)` ile `initialize` cevabı beklenir; boş pencere = hata + 120 sn geri çekilme |
| 140 | Kaldırma `CLAUDE_CODE_EFFORT_LEVEL` ve `permissions.defaultMode` için kurulum öncesi değerleri geri koymuyordu; boş model adında `strings.Fields(...)[0]` paniği; `gh issue close` otomatik kuyrukta yanlış klasörden çalışıyordu; `restart:` anahtarları 3 günde bir tekrar bildirim üretiyordu; marketplace güncellemesinden sonra statusLine eski sürüm klasörünü gösteriyordu | `managedEffort` / `managedPermissionPrevious` kaydı ve geri yükleme; `modelWords` kontrolü; `syncDoneIssues(cfg, path, cwd)`; `restart:` 30 gün; `healStatusLine` (`ensure` içinde) statusLine'ı geçerli eklenti köküne çevirir |

## 5i. v5.3 — erken sıfırlanma, görünür devam, hızlı hook, kendi kendini onarma

Kullanıcı isteği: "bazen limitleri sıfırlıyorlar yeni modeller için, anında anlayıp yarım işe devam etmeli, her araçta"; ayrıca pencere birikmesin, bağlam kaybolmasın, daha hızlı/hafif olsun, maymun bile kullanabilsin.

| # | Sorun | Çözüm |
|---|---|---|
| 141 | Bekleme yalnızca duyurulan sıfırlanma saatine kilitliydi; sağlayıcı erken sıfırlarsa oturum saatlerce boşta kalıyordu | `earlyreset.go`: hook içi bekleme ve sleeper 5 dk'da bir kullanımı tazeler (`wait.earlyResetPollMinutes`), pencere temizse bekleme anında biter (`⚡ … planlanandan önce sıfırlandı`); zamanlanmış beklemeye hafif gözcü süreç eşlik eder; başka pencereden gelen taze statusline verisi park edilmiş oturumu hemen devam ettirir (`triggerEarlyResumes`) |
| 142 | Kullanım verisi olmayan araçlarda (Droid/Copilot) sabit 30 dk'lık tekrar denemesi erken sıfırlanmayı geç fark ediyordu | `retryDelaySeconds`: 10/20/30/45/60 dk merdiveni (`wait.retryMinutes`), `stopFailureMaxAttempts` 5 |
| 143 | `noctis cancel` hook içinde uyuyan beklemeyi bitiremiyordu | `waitWatch.tick` kayıt silinince uyanır; oturum `▶ … iptal edildi` ile devam eder |
| 144 | Uzun bekleme sonrası her devam yeni bir pencere açıyordu (Windows), macOS/Linux'ta ise görünmez arka plan oturumu oluyordu | `launch.go`: Windows Terminal sekmesi (`wt -w 0 new-tab`), yoksa konsol penceresi; macOS'ta Terminal, Linux'ta masaüstü terminali (`resume.terminal`); motor başlattığı claude sürecini bekler, bir önceki kendi açtığı pencereyi kapatır (`resume.closePrevious`), eski pencerenin durum çubuğu `↪ başka bir pencerede sürüyor` der |
| 145 | Yeniden başlatma başarısız olursa kimse haber vermiyordu | `launch.failed` bildirimi + `launch-failed` günlüğü |
| 146 | Her hook'ta ~55 regex ve iki büyük katalog init'te derleniyordu (~8 ms) | `lazyre.go` (ilk kullanımda derlenen `lazyRe`), kataloglar `catalogTable()` ile tembel; süreç başlangıcı ~0,6 ms |
| 147 | Eşikten uzakken bile her `PostToolBatch` kilit + state okuma + yazma yapıyordu | `quiet.go`: sessiz hızlı yol (işaretçi + usage.json; 2 dk TTL, config/state/model/bağlam/kullanım değişince geçersiz), `updateState` değişmeyen içeriği yazmaz; hook p50 9 ms → 3–4 ms, sessiz hook hiçbir dosyaya dokunmaz |
| 148 | Bozuk `state.json`/`usage.json` yalnızca bir yazma denemesinde onarılıyordu; yalnızca okuyan hook bozuk dosyayı bırakıyordu | `readStoredState` ve `readUsageRepaired` okuma anında yedekten geri yükler, yedek yoksa dosyayı temizler |
| 149 | Runner öldürülürse `handedOff` kaydı kalıyor, oturumun prompt'ları süresiz bloklanıyordu | `clearDeadHandoffs`: süreci ölmüş devir (60 sn ödemesiz süre) serbest bırakılır, `handoff-gone` günlüğü |
| 150 | Hook `registerWait` ile `scheduleRunner` arasında öldürülürse oturum runner'sız kalıyordu | Zamanlama artık kayıttan önce (`record["scheduled"]`), üstüne `rescheduleStrandedWaits` emniyet ağı (hook, statusline, SessionStart) |
| 151 | Öldürülen süreçten kalan `.tmp` dosyaları ve tanımadığı biçimdeki kilit dosyaları birikiyordu | `sweepTempFiles` (5 dk'dan eski), `lockAbandoned` içeriğindeki ilk sayıyı pid kabul eder |
| 152 | Hata raporu için log/journal/state toplamak elle işti | `noctis report --bundle`: tek zip (ev dizini, jeton, webhook maskeli) |
| 153 | İndirilen ikilinin bozulup bozulmadığı kontrol edilmiyordu | `verifyShippedBinary`: yerleştirmeden önce `bin/SHA256SUMS`; uyuşmazsa kurulmaz. CI sürümü kaynaktan derleyip aynı toplamları doğrular ve provenance attestation ekler |
| 154 | Ayrıştırıcılar (hook girdisi, kuyruk, otomatik kuyruk, dil, kullanım payload'ı) yalnızca örnek verilerle test ediliyordu | 5 fuzz hedefi (`fuzz_test.go`), CI'da 30 sn/hedef |
| 155 | Gerçek araç sözleşmeleri sessizce değişebiliyordu | `real-tools.yml`: haftalık gerçek CLI kurulumu + bayrak/handshake duman testi |
| 156 | "Maymun testi" yoktu: rastgele sıralı, bozuk girdili, paralel, süreç öldüren kullanım senaryosu | `tests/monkey.js` (tohumlu rastgele; hook/komut/config/dosya hasarı/paralel/limit değişimi; değişmezler: panik yok, geçerli JSON, okunur gerekçesiz blok yok, runner'sız oturum yok, kilit/tmp sızıntısı yok) |

## 5j. v5.3 sertleştirme — iki bağımsız inceleme (motor + testler) ve bulunan hatalar

İki inceleme ajanı (biri Go motorunu, biri test takımını kırmaya çalıştı) + bir doğrulama turu. Bulunanların hepsi düzeltildi ve teste bağlandı.

| # | Sorun | Çözüm |
|---|---|---|
| 157 | Sessiz hızlı yol yalnızca `usage.json`'a bakıyordu; `fable.json` (OAuth/app-server yazıcısı, tazeyse öncelikli) hiç kontrol edilmiyordu → başka bir sürecin yazdığı %99 kullanım 2 dakikaya kadar gizlenebilirdi | İşaretçi üç dosyanın da içerik hash'ini (`fileStamp` = uzunluk + sha256) taşır; ayrıca `at` iki yönlü kontrol edilir (geri giden saat işaretçinin ömrünü uzatamaz) |
| 158 | `fileStamp` mtime'ı float64'te saklıyordu: 256 ns kuantumu ve 1 sn çözünürlüklü dosya sistemlerinde (HFS+, ext3, NFS) aynı saniyedeki değişiklik görünmezdi | mtime yerine içerik hash'i |
| 159 | `writeQuietMarker` durumu değerlendirdikten *sonra* hash alıyordu; arada yazılan bir bekleme işaretçiye gömülüyordu | Hash karar öncesi alınır; yazarken değişmişse işaretçi yazılmaz |
| 160 | İşaretçi saat kayması olan makinede hiç devreye girmiyordu (kaydedilen değer offset'li, karşılaştırılan ham) | İşaretçi `usage.json`'daki ham değerleri saklar |
| 161 | `launched` kaydı hiç temizlenmiyordu ve süreç adı alt-dize ile eşleşiyordu (`sh` → `sshd`, `bash`, `dash`): geri dönüşte **alakasız bir süreç öldürülebilirdi** | Kayda TTL (8 gün, en uzun beklemeden uzun), tam ad allow-list'i, süreç adı okunamıyorsa öldürme yok, `age <= 0` → `age < 0` |
| 162 | Kullanım yüzdesi `NaN` / 150 / -20 gelirse hiçbir `used >= threshold` karşılaştırması tutmuyordu — koruma sessizce kapanıyordu. `recordStatusline`, `codexWindows`, `antigravityRateLimits` `toWindow`'u atlıyordu | `sanePercent` + `saneResetTime` tek kapı; dört yazıcı da oradan geçer; `resetsAt` 45 günlük ufukla sınırlı (yoksa oturum yıllarca park edilebilirdi) |
| 163 | `state.json`/`usage.json` geçerli JSON ama nesne değilse (`null`, `[]`, `123`) yedek yok sayılıyor, tüm bekleyen oturumlar kayboluyordu; `updateState`'in yedek koruması da `json.Valid` ile aynı hatayı yapıyordu | `usableStateJSON` tek tanım; `readStoredState`, `readUsageRepaired` ve `readUsageForUpdate` aynı kuralı uygular |
| 164 | Erken sıfırlanma: pencerenin payload'dan kaybolması "sıfırlandı" sayılıyordu; boş/bozuk cevapta canlı limitin içine uyanılıyordu | Kanıt şartı: veri taze, payload'da başka bir pencere var **ve** duyurulan sıfırlanma saati geçmiş |
| 165 | `waitWatch.startedAt` hiç set edilmiyordu (ölü kod): kardeş hook yeni bir bekleme yazınca eski gözcü onu siliyordu | `newWaitWatch` beklemenin `startedAt`'ini sabitler |
| 166 | `earlyTriggeredAt` bir kez set edilip hiç temizlenmiyordu: o bekleme bir daha erken devam edemiyordu | Her yeniden planlamada temizlenir, `startedAt` tazelenir |
| 167 | `report --bundle`: config anahtar bazlı maskeleniyordu ama webhook URL'si `guard.log`/`errors.log`/`summary.txt`'ye ham düşüyordu (`url.Error` tüm URL'yi yazar), `settings-subset.json` maskelenmiyordu, desen `token`/`key` sözcüklerini kaçırıyordu | `webhookErrorText` yalnızca host + neden yazar; `configuredSecrets` yapılandırılmış sırları birebir siler; `settings-subset` de `redactSecretKeys`'ten geçer; desene `token|key|glpat|npm_|AIza|user:pass@` eklendi |
| 168 | Bozuk `SHA256SUMS` satırı `expected[:12]` ile panikletiyordu; `noctis checkpoint --sid` bayrağı yok sayılıyordu; `selftest` sahte oturum için bekleme bırakıyordu | 64-hex doğrulaması + `shortSum`; `--sid` desteklenir; selftest kendi beklemesini temizler |
| 169 | Uzun prompt'ta tekrar eden cümle checklist'e iki kez giriyordu; GitHub etiketi `highlight` → P1, `non-critical` → P0 gibi yanlış öncelikler üretiyordu | `dedupeItems`; etiket eşleşmesi sözcük bazlı + `priority/P1` desteği + bileşik etiketlerde "priority" çıpası şartı |
| 170 | Kendi düzenleme araçlarım `github.go`'ya görünmez bir kontrol baytı (0x08) yazmıştı: regex sessizce yanlış eşleşiyordu, terminalde ve `gofmt`'te fark edilmiyordu | `tests/hygiene.js`: kontrol baytı, görünmez boşluk, çalıştırma biti, `bin/noctis` launcher'ı, SHA256SUMS tazeliği ve sürüm tutarlılığı; CI'da |
| 171 | `wakeSameSession` oturumu yerinde uyandırmak için hook'un **içinde** saatlerce uyuyor, ama beklemeyi `inHook: false` diye kaydediyordu. Kendi işareti `waking` yazılıyor, hiçbir yerde okunmuyordu (ölü kod): zamanlanmış runner ile erken-sıfırlanma tetikleyicisi, üzerinde canlı bir hook uyuyan beklemeyi sahipsiz sanıp **ikinci bir devam** başlatabiliyordu — aynı iş hem yeni pencerede hem yerinde sürüyordu | `hookSleeping()`: "şu an bir hook bu beklemenin üzerinde uyuyor" tek yerden yanıtlanır (`inHook` veya `waking`); runner (grace + heartbeat), `triggerEarlyResumes` ve `/clear` üçü de aynı görür |
| 172 | `waking` yalnızca defer ile siliniyordu; yerinde uyandırma `os.Exit(2)` ile bittiği için defer hiç çalışmıyor, işaret beklemede kalıyordu → runner ölmüş bir hook'a süresiz mühlet tanıyordu | Uyandırma anında, `wakeAttemptedAt` ile aynı yazımda silinir |
| 173 | Maymun testi beklemeleri **ham** oturum kimliğiyle arıyordu; motor `safeName` ile dosya-güvenli anahtar kullanıyor → `../escape`, boşluklu ve Türkçe kimliklerde hiçbir bekleme bulunamıyor, oturuma göre süzülen her yargı yanlış sonuç veriyordu (171'i "sahte alarm" gibi gösteren buydu) | `waitKey()` harness'a eklendi, motorun kuralını birebir taklit eder; maymun onu kullanır |
| 174 | "Hook, sıfırlanmaya kadar bekledi" kontrolü kurulumdan **önce** başlayan sabit 3500 ms bütçeye dayanıyordu; yüklü makinede kurulum 1 sn sürünce sahte hata veriyordu | Beklemenin kendi `resumeAt` anına bakılır |
| 175 | `go test -fuzz` deseni hiçbir şeye uymazsa **0 ile çıkar**: yeniden adlandırılan bir fuzz hedefi CI'da sessizce çalışmamaya başlıyordu | Hijyen testi `fuzz_test.go`'daki her hedefin `ci.yml`'de tam sözcük olarak geçtiğini doğrular (tam sözcük: `FuzzXY`, `FuzzX`'i karşılamaz) |
| 176 | Soak'ın "artık kilit/tmp kalmasın" kuralı süpürücünün **5 dakikalık gerçek zaman** eşiğini hesaba katmıyordu; sıkıştırılmış bir hafta ~70 saniye sürdüğü için öldürülen bir hook'un bıraktığı `state.json.<pid>.tmp` daha süpürülemeden anomali sayılıyordu (rastgele başarısızlık) | Günlük kural yalnızca eşiği geçmiş artıkları sayar; koşu sonunda artıklar geriye tarihlenip bir status line çağrılır ve **süpürmenin çalıştığı** doğrudan doğrulanır (eşik 500 dakikaya çıkarılmış bir binary ile negatif kontrolü yapıldı) |
| 248 | Paralel koşan süitler depo `bin/`'ini paylaşıyordu: bir yeniden derleme ya da başka bir süitin `refreshChecksums()` çağrısı tam ortada gelince alakasız bir süit "gelen binary SHA256SUMS ile uyuşmuyor" diye düşüyordu — tedarik zinciri arızası gibi okunuyor, oysa iki testin aynı dizini paylaşması. Bu turda gerçekten oldu (soak'ları ben çökerttim) | Her lab kendi kaynak anlık görüntüsünü alıyor (kurulum yükü, birkaç MB) ve ondan kuruyor. On süit aynı anda koşturulup doğrulandı |

| 238 | **`registerWait`, `scheduleRunner`'ın az önce başlattığı runner'ı öldürüyordu.** Her çağıran önce zamanlıyor sonra kaydediyor; `scheduleRunnerLocked` yeni `scheduled` kaydını hâlâ durumda duran bekleme kaydının üstüne yazıyor; `registerWait`'in içindeki `cancelRunner` da durumu okuyup milisaniyeler önce doğan runner'ı öldürüyordu. Oturum bir cesede park ediliyordu: `status` devam saatiyle bekleme gösteriyor, `errors.log` temiz, `doctor` bir şey görmüyor, onarım geçişi de atlıyor (çünkü kayıtta bir `scheduled` **var**, sadece ölü). **Hiçbir şey geri gelmiyordu** — eklentinin var olma sebebi olan tek arıza. Windows'ta daha kötü: `cancelRunner` görev adını koşulsuz sildiği için **ilk duraklama bile** kendi kaydettiği görevi siliyordu | `cancelRunnerExcept` + `sameSchedule`: az önce zamanlanan runner hariç her şey iptal ediliyor. Gerçek binary'de üretilip düzeltildikten sonra doğrulandı |
| 239 | Mevcut lab testi bu senaryoyu **zaten koşuyordu** ama yalnızca *eski* pid'in öldüğünü kontrol ediyordu; yenisinin yaşadığını kimse sormamıştı. Testin yarısı vardı ve eksik yarısı tam bu hatayı saklıyordu | Kontrol tamamlandı: yeni runner yaşıyor ve eskisinden farklı |
| 240 | Durum çubuğu payload'unun şekli değişirse (alan adı değişikliği, sayı yerine ISO tarih) hiçbir pencere ayrıştırılamıyor, ama `updatedAt` yine de taze damgalanıyordu: önceki okuma taze onaylanmış gibi taşınıyor, bayatlık kuralları hiç ateşlenmiyor, "kullanım verisi yok" bildirimi hiç çıkmıyor. %99'daki bir hesap en son ayrıştırdığı değere göre yargılanmaya devam ediyordu — sessizce | Hiçbir pencere okunamadıysa `updatedAt` **ilerletilmiyor** (veri normal şekilde bayatlıyor ve API yolu devralıyor) ve günde bir kez "bu sürüm payload'u okuyamıyor, eklentinin güncellenmesi gerekebilir" uyarısı veriliyor |
| 241 | `(after #etiket)` bağımlılığı, o etiketi taşıyan **ilk** madde işaretlenince çözülüyordu — oysa sözleşme "her referans verilen madde işaretli olduğunda" diyor. Üç `#tests` maddesinden biri bitince "sürümü yayınla" uygun hale geliyor ve kuyruk modu onu gözetimsiz yürütüyordu | Artık etiketi taşıyan **hepsi** bekleniyor; bilinmeyen etiket hâlâ kilitlemiyor. İki Go testi, negatif kontrolü yapıldı |
| 242 | Kontrol listesi dosyasında kutusuz bir madde işareti, üstündeki maddenin metnine yapışıyordu (`"ship the release - some note"`). Sebep ulaşılamaz bir koşuldu: `!hasBoxes` dalında her madde işareti zaten `ok` dalına gidiyor, dolayısıyla bu kol yalnızca sondaki `\r` ile eşleşen bir dize için ateşlenebilirdi — öyle bir dize yok. Niyetin tersi oluyordu | Koşul `hasBoxes &&` olarak düzeltildi; testle sabitlendi |
| 243 | Bugün yazdığım `firstRunSetup`, üçüncü parti durum çubuğunu **kaybediyordu**: `section()` anahtar yoksa **yeni** bir harita döndürüyor, dolayısıyla onun içine yazmak bir çöp haritayı değiştiriyordu. Elle yazılmış ya da eski sürümden kalma bir config'de zincir kaydedilmiyor, diğer aracın satırı ekrandan siliniyor ve kaldırma onu geri veremiyordu | Bölüm açıkça oluşturulup config'e bağlanıyor. Laboratuvarda üretildi ve düzeltme doğrulandı |
| 244 | Kullanıcı fiyat tablosu (`report.pricing`) bir harita ve **Go harita sırasını rastgeleleştirir** — "önce en özel eşleşme kazanır" sözleşmesi uygulanamıyordu; hem `opus` hem `opus-5` içeren bir config koşudan koşuya farklı fiyat veriyordu | Anahtarlar önce uzunluğa göre sıralanıyor: belgelenen niyet artık gerçekten doğru |
| 245 | `tailOfFile` son 512 KB'ı almak için **dosyanın tamamını** belleğe okuyordu — ve hata raporu paketinden çağrılıyor, yani tam da logların en büyük olduğu anda | Yalnızca kuyruk okunuyor |
| 246 | Bir klondan `noctis ensure` çalıştırmak, çalışma ağacındaki `bin/noctis` launcher betiğini 8 MB'lık binary ile eziyordu (bu turda bir test koşusunda gerçekten oldu) | Kurulu ağaçta `go/go.mod` yok; varsa kaynak ağacıdır ve launcher'a dokunulmuyor |
| 247 | `lazyRe`'nin 7 proxy metodu hiç kullanılmıyordu (yani hiç çalıştırılmamış kod), `webhook.go`'da yeniden deneme gecikmeleri bir sabite indeksle bağlıydı (sabit büyütülürse arka planda panik) | Metotlar silindi; indeks sınırı korundu |

| 230 | Kurulum **deponun tamamını** kopyalıyordu: Go kaynağı ve testleri, JS süitleri, dokümanlar (77 KB'lık PLAN + 419 KB'lık PNG), CI iş akışları, binary'nin içinde zaten derli olan `i18n/*.json`, ve **altı platformun binary'si**. Kullanıcıya 48 MB / ~130 dosya iniyordu; gereken 7,5 MB / 16 dosya | `installedPaths()` + `copyPluginTree`: yalnızca gerekli olanlar. İki çağrı yeri de (Claude ve diğer host'lar) değiştirildi. `scripts/notify.ps1` ve `launch.ps1` listede kalmak zorunda — Windows alarmları ve pencereli devam onları çalıştırıyor ve yokluklarında **sessizce** başarısız oluyorlar; `bin/SHA256SUMS` de kalıyor, çünkü her SessionStart'ta yeniden doğrulanıyor. Ayrıca `placeBinary` ikinci 8 MB'lık kopya yerine sabit bağlantı kuruyor (başarısız olursa kopyaya düşüyor) |
| 231 | **Marketplace kurulumundan sonra eklenti hiçbir şey yapmıyordu.** `/plugin install` ağacı yerine koyuyor ve hook'ları bağlıyor, ama config.json ve durum çubuğu olmadığı için korumanın hiç kullanım verisi olmuyor: %95'te bile sessiz. Her şey kurulu görünüyor; kullanıcı bunu duvara çarpınca öğreniyor. Rakip araştırması da bunu doğruladı: en iyileri iki adım, üçüncü adım yok | `firstRunSetup`: ilk SessionStart'ta config yazılıyor ve durum çubuğu bağlanıyor (varsa mevcut olan zincirleniyor, kaldırma geri alıyor) — **korumayı çalışır kılan asgari şey, o kadar**. Model, effort ve izin modu gibi görüş bildiren ayarlar `/noctis:setup`'ta kalıyor: onlar nasıl çalışmak istediğinizle ilgili, korumanın görmesiyle değil. Doğrulandı: setup'sız kurulumda %95'te blokluyor |
| 232 | **Sürüm iş akışı her sürümde düşerdi.** `release.yml` checksum'ları kabuk tek satırıyla yeniden üretip depodakiyle `diff` alıyordu; o tek satır `bin/noctis` launcher girdisini kaçırıyor ve farklı sırada yazıyor — yani manifesti asla yeniden üretemiyordu | Aynı üretici (`tests/harness.js` → `refreshChecksums`) çağrılıp `git diff --exit-code` alınıyor. Tek üretici yoksa kontrol tiyatrodur |
| 233 | `readStoredState` **tek bir yarım okumada** yedeği geri yükleyip o yedekten sonraki her kaydı siliyordu — okuma yolundan, kilitsiz, tek satır logla. `state.json`'ı atomik olmayan biçimde yazan her şey (kullanıcının elle düzenlemesi, bir senkron istemcisi, bir betik) birkaç milisaniyelik bir pencere bırakır; o pencereye denk gelen bir okuyucu saniyeler önce park edilmiş bir oturumu yok eder. Kararsız bir test üzerinden bulundu | Yedeğe düşmeden önce dört kez yeniden okuma. Yarım yazım mikrosaniyelerde kapanır; gerçek bozulma bozuk kalır ve kurtarmaya ulaşır. İki testle sabitlendi (yarım okuma kaybettirmez / gerçek bozulma yine kurtarılır); düzeltme geri alınınca ilki düşüyor |
| 234 | Durum dosyasının **8 koleksiyonu hiç budanmıyordu** — oturum başına kayıt tutup sonsuza dek büyüyorlardı. Bu yalnızca disk değil: her hook bu dosyanın tamamını ayrıştırıyor, ve **hook süresi dosya boyutuyla doğrusal artıyor** (ölçüldü: 1 KB → 5,3 ms, 104 KB → 14,1 ms, 313 KB → 31,3 ms). Aylarca kullanan biri her araç çağrısında yavaşlıyor ve kimse iki şeyi birbirine bağlamıyor | Gerçekten geçici olanlar budanıyor (`routes` — okuyucu zaten bir saatten eskisini yok sayıyordu —, `autoResume`, `stopGuard`, `tasks`, `launchFailures`). `modelOverrides`/`modelSwitched` **bilerek dışarıda**: bunlar eklentinin verdiği bir kararın hafızası ve unutmak aynı geri dönüş bildirimini tekrar ettiriyor (lab tam bunu yakaladı). `queueTrust` da dışarıda — güven kullanıcının kararı |
| 235 | Masaüstü bildirimi olmayan bir makinede (sunucu, konteyner, bildirim servisi olmayan WSL) her alarm `errors.log`'a bir satır yazıyordu; `noctis doctor` günlüğü "kirli" gösteriyor ve kullanıcı makineye dair bir gerçeği hata sanıp arıyor | "Bildirici yok" hatası ile "bildirim başarısız" ayrıldı; ilki günde bir kez söyleniyor |
| 236 | Kendi eklediğim sabit bağlantı ile yeniden kurulum çarpıştı: `copyPluginTree` launcher'ı kopyalarken `O_TRUNC` **bağlantının içinden yazıp platform binary'sini 497 baytlık betikle değiştiriyordu**; sonraki checksum kontrolü binary'yi kurcalanmış diye bildiriyordu — doğru ama tamamen kendi açtığım yara | `copyOneFile` yazmadan önce hedefi siliyor, yerinde kırpmıyor |
| 237 | Bir alt ajan `NOCTIS_NO_EARLY_TRIGGER`'ın sleeper'ın kendi yoklamasını da kapatmasını önerdi ve ben uyguladım; lab bir senaryoda asılı kaldı. **Öneri yanlıştı**: belge bu anahtarı "izleyici ve durum-çubuğu kaynaklı devam" diye tanımlıyor, ve sleeper'ın erken sıfırlamayı fark etmesi onun tek işi. Belgelenmiş bir anahtarı genişletip özelliği tek uygulayan yoldan kaldırmışım | Geri alındı, gerekçe koda yazıldı. Kararsızlığın asıl kaynağı zaten testin istemediği bir sleeper'ı doğurmasıydı: senaryo artık `manualSchedule` kullanıyor |

| 225 | Kodun kullandığı bir `T()` anahtarı katalogda yoksa **hiçbir şey olmuyordu**: T() önce İngilizceye, o da yoksa anahtarın kendisine düşüyor — yani bir yazım hatası durum çubuğunda `wait.savedHint` yazısı olarak yayına çıkabilirdi, eksik bir çeviri ise Türkçe cümlenin ortasında İngilizce bir cümle olarak | **4 yeni Go testi**: kodun istediği her anahtar İngilizce katalogda var mı (kaynak taranarak), katalogda hiç istenmeyen anahtar var mı, yer tutucular tutuyor mu, gün adı/süre birimi listeleri her dil için tam mı |
| 226 | Yer tutucu testinin ilk hâli benim hatamdı: Go'nun indeksli yer tutucularını (`%[2]s`) tanımıyordu ve doğru çevirileri bozuk sayıyordu. Doğru değişmez sayı eşitliği değil — çeviri, çağıran tarafın hiç vermediği bir argüman konumuna atıfta bulunamaz ve tipleri tutmalı; daha kısa bir çeviri serbest | Test argüman konumlarını Go'nun kendi kuralıyla çözüyor (örtük sayaç + açık indeks) ve her konumu tipiyle karşılaştırıyor |
| 227 | Bu test hemen iki **gerçek** hata yakaladı: Japonca ve Çince'de kelime sırasını değiştirirken indekssiz yer tutucu kullanmıştım. Çalışma anında model adının yerine token sayısı, saniye yerine yön kelimesi basılacaktı — ve o dili okuyan kimse bunu hata olarak bildirmezdi | İkisi de indeksli yer tutucuya çevrildi; aynı tehlike için 14 dilin tamamı tarandı (başka örnek yok) |
| 228 | 12 dil %18'de takılıydı (295 anahtarın 56'sı) ve kataloglar `lang.go` içinde elle düzenlenen Go literalleriydi: dil başına yüzlerce girdi, grup başına tek uzun satır, kaçış karakterleri gözle. Bir anahtar eklemek on iki yeri elle değiştirmek demekti | Kataloglar **`i18n/<kod>.json`** dosyalarına taşındı; `scripts/i18n.js` `lang.go`'yu üretiyor ve hijyen testi ikisinin ayrışmadığını doğruluyor (negatif kontrolü yapıldı). **12 dilin tamamı %100'e tamamlandı** (2.868 yeni dize) ve `TestEveryLanguageIsComplete` bir eksik bırakıldığında derlemeyi düşürüyor. README'lerdeki "2 tam, 12 kısmi" iddiası gerçeğe uyduruldu |
| 229 | Hijyen testinin görünmez karakter kuralı Arapça'yı yanlış suçluyordu: RTL metinde Latin komutların doğru görünmesi için gereken yön işaretleri (U+200E/200F) **içeriktir**, hata değil | Kural daraltıldı: bu iki işaret yalnızca Arapça kataloğunda ve ondan üretilen dosyada serbest, başka her yerde hata (her iki yön de kontrol edildi) |

| 222 | **Kesilen chunked yanıt, cevap sanılıyordu.** `io.ReadAll`'ın hatası atılıyordu. Sonlandırıcı parçası gelmeden kesilen bir chunked yanıt Go'ya 200 olarak ulaşır ve elde kalan kısmi gövde çoğu zaman **hâlâ geçerli JSON**'dur: `{"limits":[]}` kusursuz ayrışır ve "hiç limit yok" demektir — yani her eşik karşılaştırması "bol bol kota var" diye okur. Laboratuvarda **%96'daki bir oturum bloklanmadı**; üstelik sessizce: taze `fetchedAt`, hata alanı boş, hiçbir logda iz yok. Content-Length'li kesilmede Go bizi koruyor, chunked'da korumuyor | Okuma hatası artık `truncated-response` olarak başarısız getirme sayılıyor. Kaos testi hem bunu hem de "sağlıklı durumda gerçekten bloklandığını" (kontrol) doğruluyor; düzeltme geri alınınca dört kontrol birden düşüyor |
| 223 | Ağ, disk ve göç arızalarının hiçbiri sınanmıyordu | **`tests/chaos.js`** (69 kontrol): 429/403/500, geçersiz JSON, yarıda kesilen gövde, chunked kesilmesi, hiç bayt gelmeden kapanan soket, TLS anlaşamaması, bitmeyen damla gövde, devasa gövde; **gerçek dolu dosya sistemi** (root `chmod 555`'i umursamadığı için izin numarası işe yaramıyor — küçük bir tmpfs son bayta kadar doldurularak gerçek ENOSPC üretiliyor); ve eski sürümlerden gelen dört `state.json` fikstürü + yarım yazılmış dosya. Her arızada değişmez aynı: koruma veri kaybedebilir ama arızayı "bol kota var"a çeviremez ve park edilmiş oturumu düşüremez |
| 224 | Disk dolduğunda ne olduğu hiç görülmemişti | Gerçek ENOSPC altında doğrulandı: checkpoint ve `state.json` yazımı başarısız oluyor, oturum **park edilmiyor**, kullanıcıya "duraklatma kaydedilemedi — hiçbir şey duraklatılmadı; `noctis doctor`" deniyor ve `errors.log`'a tam iz düşüyor. Önceki turun `registerWait` düzeltmesi gerçek arıza altında tutuyor |

| 219 | "Sıcak yolda yazma amplifikasyonu" ölçüldü ve **yoktu**: sakin bir hook `state.json`'a hiç yazmıyor (`updateState`'in no-op elemesi çalışıyor), yalnızca 1–6 okuma yapıyor. Asıl amplifikasyon **soğuk yolda**: bir bekleme park edilirken 3 ayrı tam dosya yazımı. Bunlardan ikisi kasıtlı ve doğru (zamanlama, kilidin *dışında* olmak zorunda — kilit içinde kabuk çağırmak bu turda düzeltilen hatanın ta kendisiydi), dolayısıyla riskli kodu teorik bir kazanç için elleme kararı alındı | Ölçüm testle sabitlendi: art arda beş no-op güncelleme `state.json`'ı (ve yedeğini) **hiç** yazmamalı, gerçek bir değişiklik ise yazmalı. İkincisi olmadan ilki bozuk bir yazıcıyla da geçerdi |
| 220 | Ölçümün kendisi ilk turda yanlıştı: `strace -e trace=rename` hiçbir yazım görmüyordu çünkü Go'nun `os.Rename`'i Linux'ta **`renameat2`** çağırıyor. "Sıfır yazım" sonucu bu yüzden anlamsızdı ve ancak pozitif kontrol (limitte bekleme park et, yazım olmak zorunda) sayacın bozuk olduğunu gösterdi | Sayaç atomik yazımın geçici dosyasını sayacak şekilde düzeltildi ve her ölçüm öncesi pozitif kontrolden geçirildi |
| 221 | `hooks.json` onarımı ile durum çubuğu onarımı "hangi binary doğru" sorusuna **ayrı ayrı** cevap veriyordu: biri çalışan çalıştırılabiliri, diğeri eklenti kökündeki kopyayı. PATH'e kopyalanmış bir binary'den `noctis ensure` çalıştırınca iki dosya iki farklı binary'yi gösteriyor; otomatik güncelleme bunlardan birini budayınca eklentinin yarısı artık var olmayan bir sürümü işaret ediyor. Laboratuvarda üretilip doğrulandı | Tek kural: `healTargetBinary()` — eklenti kökündeki kopya varsa o kazanır (marketplace'in güncellediği kopya odur), yoksa çalışan çalıştırılabilir. Her iki onarım da bunu kullanıyor; test sabitliyor |

| 217 | İncelemenin istediği "decide'ı I/O'dan ayır" işine girince asıl durum ortaya çıktı: karar çekirdeği (`evaluate`, `nearEdge`, `edgePollSeconds`, `dailyBudgetStatus`, `windowHit`) **zaten saftı** — eksik olan ayrıştırma değil, testti. Büyük ve riskli bir yeniden yapılandırma yerine yalnızca test edilemeyen parça çıkarıldı | `planNotices` ayrı bir saf fonksiyona alındı (bildirimler + "bildirildi" damgaları; damgaları yazmak yerine döndürüyor, böylece durum yazımı tek yerde kalıyor). **14 yeni Go testi**: eşikte tam durma, kapalı eşiğin "sıfırda dur" diye okunmaması, sıkıştırma öncesi erken durma, kenara yaklaştıkça sıkılaşan tazelik, bütçe muhasebesi, hardStop'un mesaj değil bekleme üretmesi. Eşik karşılaştırmasını `>` yapınca ve bildirim damgası eski haline dönünce testler düşüyor (negatif kontrol) |
| 218 | `planNotices` çıkarılırken eski kodda bir kusur görüldü: bildirim **aslında gönderilmese bile** "bildirildi" damgası basılabiliyordu (limit bildirmeyen bir host'ta config hatası bildirimi ateşlenince oturum "kullanım verisi yok" için de damgalanıyordu). Damga TTL'i dolana kadar, kullanıcı limit bildiren bir host'a geçse bile o bildirimi bir daha almıyordu | Damga yalnızca bildirim gerçekten gönderildiğinde basılıyor; test bunu sabitliyor |

| 213 | **launchd dakikaya kırpıyordu.** `StartCalendarInterval`'ın saniye alanı yok; 14:32:45'lik bir sıfırlama {Hour 14, Minute 32} olarak yazılınca **14:32:00'da**, yani limit sıfırlanmadan 45 sn önce ateşliyordu — devam eden oturum doğruca az önce beklediği limite giriyordu. macOS'ta hiç koşturulmadığı için görülmemişti | `scheduleAtMinute` artık **yukarı** yuvarlıyor (en fazla bir dakika fazladan bekleme, "erken devam etti" sınıfının tamamı kapandı). Dört alt durumla test edildi; eski davranış geri konduğunda testler düşüyor |
| 214 | **systemd monotonik timer kullanıyordu.** `--on-active=<gecikme>s`, `systemd.timer(5)`'ın deyişiyle "askıya alma sırasında duraklatılan" CLOCK_MONOTONIC üzerinde çalışır: beş saatlik bekleme için kurulup üç saat kapalı kalan bir dizüstü **üç saat geç** ateşler — ve kapağı kapalı gece beklemesi bu eklentinin en sık senaryosu | `--on-calendar=<mutlak zaman>`: gerçek zaman saati askıda durmaz ve uyanışta systemd "uyurken tetiklenen bütün timer'ları yakalayıp işler". Ayrıca Windows'un `-WakeToRun`'ıyla parite için `WakeSystem=true` eklendi; `--user` yöneticisinin `CAP_WAKE_ALARM`'ı olmayabileceği için reddedilirse bayraksız tekrar deneniyor (zamanlamanın tamamını kaybetmek yerine) |
| 215 | Yıl alanı olmayan launchd plist'i **her yıl** tekrar ateşler; tamamlanmış bir beklemeden sonra geride kalan plist oturumu bir yıl sonra, belki artık var olmayan bir dizinde yeniden başlatır | `cancelLaunchd` `bootout` ne derse desin plist'i siliyor; gerekçe koda yazıldı |
| 216 | Zamanlayıcı kodu hiç test edilmemişti: üç backend'den ikisi yazarın sahip olmadığı işletim sistemlerinde | Üretim saf fonksiyonlara ayrıldı (`launchdPlistBody`, `systemdRunArgs`, `scheduleAtMinute`) ve `runScheduler` tek enjeksiyon noktası oldu. **11 yeni Go testi**: plist gerçek bir XML ayrıştırıcısıyla doğrulanıyor (alt dize eşleşmesi bozuk etiketi görmez, launchd'in bozuk plist'e tek yanıtı ise sessizce yok saymak), kaçış karakterleri, argüman sırası, wake geri düşüşü, reddedilen backend'in yutulmaması, ve tam `zamanla → duruma yaz → iptal et` döngüsü (iki kez zamanlayınca ilk timer duruyor mu). Go kapsamı %8.7 → %11.7 |

| 212 | Süitin tamamı payload'u doğrudan `noctis hook`'a boruluyordu — yani Claude Code'un asla kullanmadığı tek yol. Manifest ile binary arasındaki sözleşme (komut yolu, args dizisi, olay adları) hiçbir zaman sınanmamıştı; koparsa koruma sessizce kapanıyor ve bütün testler yine geçiyordu | **`tests/contract.js`**: `hooks/hooks.json` okunuyor, `${CLAUDE_PLUGIN_ROOT}` çözülüyor ve 13 girdinin her biri host'un çağırdığı gibi çalıştırılıyor (109 kontrol). Manifest tutarlılığı (komut yolu kurulumdan sonra var mı ve çalıştırılabilir mi, args boş mu, matcher geçerli regex mi, olay adı binary'nin dağıtım tablosunda var mı), çıkış kodu, stdout'un tek bir JSON nesnesi oluşu, `hookEventName` eşleşmesi ve sürüm uyumu. Argümanlar düşürüldüğünde **kararın değişmediği** hem sakin hem de limitte senaryoda karşılaştırmalı doğrulanıyor. Negatif kontrolleri: tanınmayan olay, yanlış komut yolu, bozuk regex ve 5.3.0'ın P0 hatası geri konmuş bir binary — dördü de yakalandı |

| 211 | Paketleme sırasında iki kaçak derleme çıktısı (`go/noctis`, `go/bin/linux-amd64/noctis`, toplam 22 MB) depoya girmişti: strip'lenmemiş, `SHA256SUMS`'ta olmayan, hiçbir şeyin fark etmeyeceği binary'ler — `go build -o bin/...` komutunun yanlış dizinden çalışmasının sonucu | Silindi, `.gitignore`'a eklendi ve hijyen testi artık `bin/` dışındaki 1 MB'tan büyük çalıştırılabilir dosyaları hata sayıyor (negatif kontrolü yapıldı) |

| 210 | Soak, alt-ajan kapısı ya da batch **🔁 model değiştir** gerekçesiyle reddedildiğinde bunu duraklama sayıp bekleme kaydı arıyordu; oysa model değiştirme bir duraklama değil, hiçbir şey zamanlanmıyor ve kayıt yazılmıyor (doğru davranış). Bu tur akışı kaydırınca ortaya çıktı ve üç koşuda aynı oturumda tekrarlandı — yarış değil, yanlış değişmez | 🔁 artık model değişimi olarak işleniyor (PostModelSwitch + devam). Ayrıca "wait kaydı yok" anomalisi artık **gerekçe metnini ve karar günlüğü izini** taşıyor: izi olmayan duraklama anomali, olan (hook içinde biten ya da runner'ın temizlediği bekleme) değil |

| 179 | Host `hook` argümanını düşürürse (manifest `args` dizisini onurlandırmayı bırakırsa) binary `status`'a düşüp **exit 0** veriyordu: koruma kapalı, her sinyal "iyi" diyor, durum çubuğu ayrı mekanizmadan çalışmaya devam ettiği için kullanıcı fark etmiyor | Stdin'de payload varsa çağrı hook olarak **kurtarılıyor** (koruma sürüyor) ve çıktıya günde bir "host komut argümanı olmadan çağırdı — noctis doctor" uyarısı ekleniyor. Terminalden argümansız çalıştırma TTY kontrolüyle eskisi gibi status |
| 180 | `--help` / `-h` yoktu; `noctis --help` durum tablosu basıyordu | Gerçek yardım: komut listesi + birer satır açıklama, iki dilde |
| 181 | Panic kurtarma `initPaths`/`loadConfig`/`resolveHost`/`setLocale`'den **sonra** kuruluyordu; oradaki bir panik exit 2 veriyor, Claude Code bunu *bloklayan* hook olarak okuyordu | `defer recover()` `main()`'in ilk satırı |
| 182 | `hooksLookDead` hiç hook gelmemişse (`lastHookAt == 0`) asla uyarmıyordu: hook'ları en baştan hiç çalışmamış bir kurulum sonsuza kadar sessiz | En eski durum çubuğu örneğinden yaşlandırılıyor; aynı örnek sayısı ve tazelik barajlarıyla |
| 183 | `pruneState` bayat beklemeyi silerken zamanlayıcı kaydını bırakıyordu; launchd plist'inde yıl yok (yıllık tekrar) | Silinen beklemelerin kayıtları **kilidin dışında** iptal ediliyor; runner boş beklemeyle uyandığında systemd timer'ını da siliyor |
| 184 | `selfcheck` mesajı `output["systemMessage"]`'i join etmeden **eziyordu**; ezilen şey depodan gelen `TASKS.md`'nin oturumu sürüklediğini söyleyen tek uyarıydı ve "bildirildi" damgası mesaj atılmadan basıldığı için 7 gün susuyordu (kodun kendi yorumu bunu yasaklıyordu) | Kardeşleri gibi `joinNotices`; uyarı en sona iliştiriliyor ve damga ancak mesaj çıktıya girince basılıyor |
| 185 | Depodan gelen `TASKS.md` onaysız yönerge oluyordu ("maddeleri sırayla yap, sorma") — klonlamak ya da arşiv açmak yeterliydi | `queue.requireTrust` (varsayılan açık): güvenilmeyen dosya için yönerge enjekte edilmiyor, **her oturumda** söyleniyor; `noctis queue trust \| untrust \| status`. Eklentinin kendi yazdığı otomatik kuyruklar muaf |
| 186 | `bypassPermissions` ve `dontAsk` config'den gözetimsiz devam ettirmeye geçebiliyordu; iki README'de "asla kullanılmaz" yazıyordu | `knownPermModes`'tan çıkarıldı, `refusedPermModes` uyarı yazıp `acceptEdits`'e düşüyor; README gerçeğe uyduruldu |
| 187 | `copilotAllowAllTools` kod varsayılanı **true** ve `config.default.json`'da yok: her gözetimsiz Copilot devamı `--allow-all-tools` alıyordu | Varsayılan `false` ve config'de görünür; lab hem varsayılanı hem opt-in'i kontrol ediyor |
| 188 | `NOCTIS_USAGE_URL` herhangi bir https host'a OAuth bearer token gönderiyordu (host pin yok) | Yalnızca gerçek endpoint'in host'u veya localhost kabul ediliyor |
| 189 | `resolvePluginRoot`'taki `\|\| candidate == os.Getenv(...)` ilk turda her zaman doğru olduğu için `NOCTIS_PLUGIN_ROOT` **hiç doğrulanmıyordu**; o kök çalıştırılan script'leri ve config varsayılanlarını sağlıyor | İki değişken de aynı şekilde doğrulanıyor, uymayan reddedilip loglanıyor |
| 190 | Prompt taşıyan her şey 0644, dizinler 0755 (tüm kod tabanında tek bir 0600 vardı) | state, checkpoint, kuyruk, resume çıktısı, loglar, yedekler 0600; dizinler 0700 |
| 191 | `safeName` 80 karakterde kırpıyordu → uzun yollarda **çakışma** (güven kararı için tehlikeli); `..` olduğu gibi geçiyordu | Kırpma artık hash ekliyor, nokta dizileri nötrleniyor; birim testi çakışmayı ve `..`'yı kontrol ediyor |
| 192 | `withFileLock` 3 sn sonra ve kilit açılamadığında **kilitsiz devam ediyordu**; `updateState` tüm dosyayı oku-değiştir-yaz yaptığı için kaybeden sürecin *tüm* mutasyonu (kaydedilmiş bekleme dahil) sessizce yok oluyordu | Fail-closed: kilit alınamazsa yazılmıyor ve `errors.log`'a hata düşüyor. `registerWait` yazımın diske indiğini doğruluyor; inemezse oturum **park edilmiyor**, kullanıcıya görünür mesaj gidiyor |
| 193 | `pruneState` kilidin içinde 5 sn timeout'lu `git update-ref` çalıştırabiliyordu — diğer her sürecin 3 sn'lik sabrından uzun; hepsini aynı anda kilitsiz yazmaya itiyordu | Snapshot silme kilidin dışına kuyruklandı |
| 194 | `killDetached` saklanan pid'i kimlik kontrolü olmadan öldürüyordu (pid'ler iki günde geri dönüşüyor); oysa `closePreviousLaunch`'ta tam bu koruma vardı | `processAlive` + `ownHelperProcess` kontrolü; tanınmayan pid uyarıyla bırakılıyor |
| 195 | `quiet.go`'daki güvenlik kontrolü `interruptedWaits`'i (bir **liste**) `getMap` ile okuyordu → koşul yazıldığı günden beri sonsuza kadar false; sessiz yol tam da beklemeleri sürekli bölünen oturumlar için seçiliyordu | Liste olarak okunuyor, ayrıca öğrenilmiş kapak da kontrol ediliyor. **Ve bu sınıf artık derleyiciye yakalatılıyor:** hijyen testi her state anahtarının okuma/yazma şeklini `emptyState()` şemasıyla karşılaştırıyor (negatif kontrolü yapıldı) |
| 196 | `settings.json` kilitsiz oku-değiştir-yaz ediliyordu; `healStatusLine` her SessionStart'ta çalıştığı için aynı anda açılan pencereler birbirinin değişikliğini siliyordu | `settingsLock` + `withSettings()`; tüm yazıcılar oradan geçiyor |
| 197 | Config birleştirmesi tek seviye derindi: `{"queue":{"github":{"closeOnDone":true}}}` yazmak yanındaki varsayılanı düşürüyordu (üç çağrı yeri bunu elle telafi ediyordu) | `mergeDefaults` her derinlikte birleştiriyor; listeler değiştiriliyor, birleştirilmiyor |
| 198 | Otomatik güncellemeyle gelen yeni config anahtarları mevcut `config.json`'a hiç eklenmiyordu (`runEnsure` dokunmuyordu) | `runEnsure` eksik anahtarları ekliyor; mevcut değerlere dokunmuyor |
| 199 | `verifyShippedBinary` manifestte kayıt yoksa **fail-open**'dı; CI `SHA256SUMS`'ı karşılaştırmıyor **yeniden üretiyordu** (swap edilmiş bir binary + eşleşen manifest CI'dan geçerdi) | Kayıt yoksa reddediliyor; CI artık `diff` yapıyor |
| 200 | Başarısız devam ettirme sonrası `noctis status` "Waiting: none" diyordu — başarıyla bire bir aynı | `launchFailures` kaydı tutuluyor, `status` "!! … otomatik devam başarısız oldu — claude --resume <id>" satırı basıyor, başarılı devam kaydı siliyor |
| 201 | `doctor` sorunları sayıyor ama çözüm söylemiyor ve hep exit 0 veriyordu; `scheduler backend: sleeper` normal durumken hata gibi okunuyordu | Her sorunun altında çözüm satırı, sonda özet, sorun varsa exit 1; sleeper normal olarak işaretlenip açıklanıyor |
| 202 | 14 dilden 12'si %21 çeviriyken README "14 dil" diyordu; "bip" sesi hiçbir platformda yoktu | README'ler gerçeğe uyduruldu ("2 tam, 12 kısmi"; "masaüstü bildirimi") |
| 203 | macOS quarantine / SmartScreen / exec biti hiçbir yerde belgelenmemişti — en sık "sessizce hiçbir şey yapmıyor" kurulum hatası, üstelik kullanıcı `noctis doctor`'ı da çalıştıramıyor | İki README'ye "Hiçbir şey olmuyorsa" bölümü: `xattr -dr`, Unblock, `chmod +x`, doğrudan `noctis version` |
| 204 | `install.ps1` README'nin o kurulum yolu için listelediği `--permissions keep` / `--preset` / `--updates keep` bayraklarını geçirmiyordu | Üçü de geçiriliyor |
| 205 | `/plugin uninstall` önce çalıştırılırsa `permissions.defaultMode: auto` kalıcı kalıyor ve geri alacak binary siliniyor | Sıra README'de vurgulandı + elle geri alma (hangi anahtarlar, hangi yedek) yazıldı |
| 206 | Ortam değişkenlerinin 15'inden 9'u belgeliydi ve "yalnızca test" deniyordu, oysa hepsi canlı | REFERENCE'ta tam tablo, her birinin gerçek etkisiyle |
| 207 | Test kusurları: `history.length >= 4` kilitlemenin kaybettiği örnekleri tolere ediyordu (doğrusu 6); "runner geri geldi" kontrolü `'manual'` (= hiç runner yok) değerini kabul ediyordu; `TestLooksLikeSessionProcessNames` üretim fonksiyonunu **çağırmıyor**, normalizasyonu kendi tekrar ediyordu | Üçü de gerçek iddiaya çevrildi; `looksLikeSessionName` saf fonksiyon olarak ayrıldı ve test onu çağırıyor |
| 208 | Soak kendi kapsama sayaçlarını (`fableSwitches`, `chaosInjections`, `stops` …) basıyor ama hiçbirini iddia etmiyordu: bir özellik tamamen kırılsa da "SOAK PASSED" derdi | 7+ günlük koşularda bu sayaçların alt sınırları iddia ediliyor |
| 209 | Maymun `doctor`'ı "yalnızca exit 0" listesinde tutuyordu; `doctor` artık bir kapı | `check` gibi kapı olarak tanındı (0/1), ayrıca raporun gerçekten basıldığı kontrol ediliyor |

| 178 | Üç lab kontrolü, **detached** başlatılan bir yeniden başlatmanın günlüğünü komut döner dönmez okuyordu (yarış) ya da sabit 20 sn bütçeyle bekliyordu; yüklü makinede sahte hata veriyorlardı | `callsMatching()` / `anyCall()`: satır gelene kadar beklenir (60 sn tavan). Kontrolün sorduğu metni beklemek onu kendiliğinden doğru yapacağı için, "ne başlatıldı" kontrolü yalnızca *bir* başlatma gelmesini bekler |
| 177 | Harness mock sunucunun port **dosyasının oluşmasını** bekliyordu, içine yazılmasını değil; paralel koşuda dosyayı boşken görüp "mock server did not start" ile çöküyordu (5 sn bütçe de yüklü makinede kısaydı) | Port okunabilir olana kadar beklenir, süreç erken ölürse çıkış kodu bildirilir, bütçe 30 sn |

Test tarafı: hiç başarısız olamayan 6 lab kontrolü gerçek iddialara çevrildi (biri "ölü devir" özelliği tamamen silinse bile geçiyordu); senaryolar arası config/bekleme/kapalı-mod sızıntısı artık isimli hata; soak'ın "asla %100 üstü" sözleşmesi eşik+4 puana çekildi, 11 sessiz kaos türüne iddia eklendi, hard modda kaybolan bekleme artık anomali, p95 gecikme ve yedekten kurtarma kapıya bağlandı; maymun testi kullanım değerlerini ve ölü süreç kayıtlarını denetliyor, komutların çıkış kodunu kontrol ediyor, CI'da koşuyor; 5 fuzz hedefine panik dışında değişmezler eklendi; 8 yeni Go birim testi (`core_test.go`).

| 249 | Kullanım payload'ının iki şekli **iki ayrı ayrıştırıcıyla** okunuyordu ve hangi anahtarın kazandığı konusunda ters karar veriyorlardı; ikisi de anahtarın *var olmasına* bakıp değerin geçerliliğine bakmıyordu. `"percent": null, "utilization": 87` gelen bir payload'da cevap yanı başındayken pencere tamamen düşüyordu — ve göremediği pencereyi koruma savunamaz | Tek anahtar listesi, tek sıra, *ayrıştırılabilen* ilk anahtar kazanır. `used` bilerek dışarıda: bazı payload'larda token sayısı ve %100'e kırpılıp yeni başlamış oturumu duraklatırdı |
| 250 | `fable.json` son hatayı sonsuza dek saklıyor ve yenileyici fetch yapmadan döndüğünde (poll penceresi, backoff, kilit başkasında, kaynak kapalı) çağıranlar onu "yenileme az önce başarısız oldu" diye okuyordu. Eşiğe yakınken tek hook içinde beş uykuluk kör sondaja yolluyordu; kaynak kapatılınca kalıcı olarak donuyordu | Hata yalnızca kendi backoff'u sürerken canlı sayılıyor. Kör sondaj da "hata yok mu" yerine "yeni veri geldi mi" diye soruyor |
| 251 | "Bu oturum hangi modelde" sorusuna **iki fonksiyon** cevap veriyordu ve farklı isimler dönebiliyordu; `noctis check` fallback modeldeki bir hesap için 11 ("eşik aşıldı") dönüyor, hook'lar kişiye zaten kullandığı modele geçmesini söylüyordu. Altındaki asıl hata: oturum model geçersiz kılması 3 günde siliniyordu ama haftalık park 7 gün sürüyor — park dönüşü oturum hangi modelde olduğunu unutmuş oluyordu. Ayrıca aynı state üzerinde **iki budama döngüsü** vardı ve eskisi yenisinin yorumunun tam tersini yapıyordu | Tek çözümleyici, tek budama döngüsü; geçersiz kılma en uzun beklemeden uzun yaşıyor |
| 252 | `hostSpec.modelSwitch` ve `hostSpec.stopFailure` yazılmış, host başına set edilmiş, **hiç okunmuyordu**. Beş host'un üçünde plugin, host'un okumadığı bir `settings.json`'a `model` anahtarı yazıp **onu geri okuyup gerçek sanıyordu**; durum satırı model adı bildirmeyen bir host'ta config primary'sine düşüldüğü için kapsamlı desen varsayılan olarak eşleşiyor ve hiçbiri doğru olmadan tetiklenebiliyordu | Bayraklar bağlandı; çare yoksa kapsamlı pencere özel değil — sıfırlanana kadar park ediyor. Okunmayan yetenek bayrağını build'i kıran bir test yakalıyor |
| 253 | Budama yalnızca state yazılırken çalıştığı için **salt-okunur her yüzey motorun çoktan attığı kayıtları gösteriyordu**: `noctis checkpoint` tüketilmiş/süresi geçmiş checkpoint'i tam bir devam belgesi olarak basıyordu (`--sid` kuralları daraltmak yerine değiştiriyordu), `status` ve durum satırı gelmeyecek resume sözü veriyordu, `doctor` state'i hiç okumadığı için `noctis off` sonrası bir saat boyunca "her şey yolunda" deyip 0 dönüyordu — ve `selftest`'in sonda hook'u *koruma kapalı olduğu için* 0 ile çıkıp **geçen kontrol** sayılıyordu | Tek `checkpointUsable`, tek `waitLive`; komutlar kopya taşımak yerine bunları soruyor. Kapalı koruma artık doctor'da hata satırı ve selftest onu asla yeşil saymıyor |
| 254 | Checkpoint, güvenilmeyen bir `TASKS.md`'nin maddelerini **güven kapısını sormadan** kendi markdown'ına kopyalıyordu — amacı resume'da modele geri verilmek olan bir belgeye | `queueTrusted` checkpoint yazımında da soruluyor |
| 255 | Dosya sonu okumanın **üç ayrı gerçeklemesi** vardı, ikisi hatalı: hata raporu paketindeki her kısaltılmış dosya satır ortasından başlıyordu (kırpma, okuyucunun imkânsız kıldığı bir koşulun arkasındaydı — `decisions.jsonl` için bozuk bir JSON satırı), stat ile okuma arasında küçülen dosya tamponun kalanını NUL olarak bırakıp "satır" diye ayrıştırıyordu, bir okuyucu da EOF'u **metin olarak** karşılaştırdığı için sarmalanmış EOF'ta transcript özeti sessizce boş dönüyordu | Tek ilkel (`readTailBytes`) + `dropPartialFirstLine`; üçü de onun üstünde |
| 256 | `report` çıktısı tekrar üretilebilir değildi: model sıralaması kararsız sıralama + berabere kalabilen karşılaştırıcı kullanıyordu, iki eşit toplam koşudan koşuya yer değiştiriyordu. Sürüm bileşeni ayrıştırılamayınca sessizce 0 oluyordu — `2.1.251-beta` 2.1.0 okunup test edilen minimumdan yeni bir Claude Code "çok eski" raporlanırdı | İsimle beraberlik bozma; `leadingNumber` |
| 257 | İşletim sistemi zamanlayıcı yolu (systemd/launchd/schtasks) **hiç çalıştırılmamıştı** — bu turun iki zamanlayıcı hatası da tam bu boşlukta yaşamıştı | `tests/scheduler.js`: PATH'e katı bir `systemd-run` taklidi konuyor, pluginin *kendi* arka uç tespiti onu seçiyor, park edilmiş oturum kimse bakmazken gerçekten zamanlayıcıyla geri geliyor. Plist, Apple'ın kendi ayrıştırıcısı `plistlib` ile doğrulanıyor. İki tarihsel hata geri konduğunda süit ikisini de yakalıyor. Yeni `noctis schedule-preview` ne kaydedileceğini basıyor, hiçbir şey kaydetmiyor |
| 258 | Kapsam olarak duyurulan %13.5 yanlış soruyu cevaplıyordu: `go test`'in tek başına ulaştığı yer orası, oysa kararların neredeyse tamamını lab'in alt süreç olarak çalıştırdığı derlenmiş ikili veriyor ve bu birim-test sayaçlarına hiç yansımıyor | `go build -cover` ikilisiyle lab koşup birim profiliyle birleştirilince **%80,5**. `node tests/coverage.js` sayıyı yeniden üretiyor ve hiçbir testin ulaşmadığı fonksiyonları listeliyor. Ölü kodun üç şekli (okunmayan yetenek bayrağı, yazılıp okunmayan alan, geçirilip kullanılmayan parametre) artık build'i kıran testlerle korunuyor |


## 5k. v5.5.1 — süitin ilk kez gerçek Windows ve macOS makinelerinde koştuğu tur

5.5.0, hiçbir yerde Linux dışında koşmamış bir süitle çıktı. Aşağıdakilerin tamamı, süit ilk kez
gerçek Windows ve macOS koşucularına konulup **tek bir beklenti gevşetilmeden** yeşile çekilmeye
çalışılırken bulundu. Onikisi de ürün hatası; testlerin taşıdığı Linux varsayımları ayrıca
düzeltildi ve sürüm notlarında listelendi.

| # | Sorun | Çözüm |
|---|---|---|
| 259 | Windows'ta park edilen oturum hiç geri gelmiyordu: bekleme kaydı runner'dan **sonra** yazıldığı için sleeper boş bir `waits` haritasına doğup anında çıkıyordu; süreç başlatmanın yavaş olduğu yerde pencere her seferinde açılıyordu | Bekleme önce kaydediliyor, sleeper kendi kaydını bekliyor; devir tek kilitli yazımda (`registerWait`, `736b9a3`) |
| 260 | Kilit, onu **hâlâ tutan** bir süreçten alınabiliyordu: bekleyen pid'i okuyup sahibi ölü sanıyor, bu sırada kilit el değiştiriyor ve canlı sahibin kilidi siliniyordu (12 paralel yazımın 6'sı kayboluyordu) | Ölü sahip için 2 sn eşik (`lockDeadOwnerMs`), silmeden hemen önce sahip yeniden okunuyor (`holderStale`) |
| 261 | Windows'ta **her okuma, yarıştığı yazmayı engelliyordu**: Go dosyaları `FILE_SHARE_DELETE` olmadan açıyor, dolayısıyla açık bir okuyucu hem `rename`'i hem `delete`'i bloke ediyordu. Kilidin salınamaması `ERROR_ACCESS_DENIED`'a, `state.json`'ın değiştirilememesi kilidi 10 sn'den uzun tutmaya ve bekleyenlerin toplu vazgeçmesine yol açıyordu | Tek ilkel `readFileShared`: Windows'ta üç paylaşım bayrağıyla `CreateFile`, diğer platformlarda `os.ReadFile`. Sıcak yoldaki altı okuyucu (JSON okuyucu, kilit sahibi yoklaması, `fileStamp`, pid dosyaları, usage yedeği, runner launcher) ona taşındı |
| 262 | Rename hedefi değiştirirken gelen okuyucu `ERROR_SHARING_VIOLATION` alıyor, okuyucu bunu **bozuk içerikten ayırt edemiyor** ve `state.json`'ı yedekten geri yüklüyordu — yani sessizce bir önceki yazıma dönüyordu (tek hesapta 58 kez) | Geçici açılamama yeniden deneniyor; yedek yalnızca dosya **okunup** ayrıştırılamadığında geri yükleniyor (`strictRead.unopened`); hiç açılamayan bir state üzerine boş state yazılmıyor |
| 263 | Yük altında state güncellemeleri düşüyordu: `withFileLock` sırada **ilerleme olsa bile** sabit sürede vazgeçiyordu; ayrıca her resume, beklemeyi temizlemek ve checkpoint'i tüketmek için iki ayrı kilitli yazım yapıyordu | Süre, sıranın uzunluğunu değil **ilerlemesizliği** ölçüyor; arka plan süreçleri (`sleeper`, `resume`) için üst sınır 15 dk; temizleme ve tüketme tek yazımda |
| 264 | Yük altında relaunch başlatacağı aracı bulamıyordu: her başlatmada `where.exe`/`which` çağrılıyor, 5 sn zaman aşımı doluyor ve oturum geri gelmiyordu (tek koşuda 311 hata) | `exec.LookPath` süreç başlatmadan cevaplıyor; eski davranış yalnızca yedek yol olarak `probeExecutable`'da |
| 265 | Windows her park'ta hiçbir şeyi silmek için bir PowerShell süreci ödüyordu: `Unregister-ScheduledTask` koşulsuz çalışıyordu — sleeper arka ucunda böyle bir görev hiç yoktu, görev varken de hemen ardından gelen `Register-ScheduledTask -Force` zaten onu değiştiriyordu. Üstelik bu bedel **global zamanlama kilidinin içinde** ödeniyordu | Kayıt yoksa atlanıyor (`scheduledWithoutTask`); yerine görev kaydedilecekse de atlanıyor, kayıt başarısız olursa eski görev oracıkta siliniyor |
| 266 | Kaydettiği bir runner'ı öldürmeden önce pid'in kendi yardımcısı olduğunu doğrulama kontrolü, Windows'ta `tasklist` ile **makinedeki tüm süreçleri** listeliyordu — yine aynı global kilidin içinde | `CreateToolhelp32Snapshot` + `Process32Next` aynı soruyu süreç içinde cevaplıyor; Unix `ps`'te kalıyor |
| 267 | Planlanandan önce sıfırlanmayan bir pencere kullanıcıya "planlanandan önce sıfırlandı" diye duyuruluyordu | Karşılaştırma paylaşılan yüklemden (`windowClearedAt`) çağırana taşındı; üç birim testi bu yüklemin doğru davranışını zaten koruyordu |
| 268 | Onarım geçişi, onarmaya çalıştığı hook'la yarışıyordu: bekleme kaydı yazıldıktan sonra zamanlama ikinci bir yazımda geldiği için arada kayıt "runner'sız" görünüyor ve onarım ikinci bir runner planlıyordu (tek hesapta 43 kez) | 30 saniyelik doğum payı (`schedulingGraceSeconds`): oluşturulmakta olan bir runner, oluşturulamamış sayılmıyor |
| 269 | Arka plan süreci, yalnızca **yavaş** olan bir kilitten vazgeçiyordu: iki süreden biri (toplam sıra sınırı) kimin beklediğini soruyordu, diğeri (10 sn ilerlemesizlik) herkes için aynıydı; makine tıkandığında önce o doluyor ve resume'un yazımı düşüyordu | İlerlemesizlik süresi de kimin beklediğini soruyor; hook'un 10 saniyesi duruyor, sleeper/resume canlı sahibin tutabileceği kadar bekliyor. Bekleme uzayınca yoklama aralığı da geriliyor (2 sn'den sonra 3×, 10 sn'den sonra 8×) |
| 270 | Saat kayması kontrolü saati değil **hook'u** ölçüyordu: sunucunun `Date` başlığı, çağıranın elindeki `now` ile karşılaştırılıyordu; o da hook'un başlangıç zamanı, yani aradaki kilit beklemesi dâhil her şey "kayma" sayılıyordu. Hata tek yönlüydü (geçen süre daima pozitif) ve bu sahte fark sıfırlanma zamanlarına ve ETA'ya ekleniyordu | İstek kendini damgalıyor (eklentinin saati + monotonik tur süresi); kayma, `Date` ile bu aralığın orta noktası arasındaki fark. Tur süresi `fetchTimeout`'u aşarsa örnek güvenilmez sayılıp bilinen değer korunuyor. Altı birim testi eklendi |

## 6. Test listesi

`tests/lab.js` — sahte `claude`, ayrı süreçte sahte kullanım API'si (saat kayması simülasyonu dahil), sıkıştırılmış saatler; 179 kontrolle başladı, bugün 614 kontrol (hepsi geçiyor): bayat veri öngörüsü, Stop kapısı, temiz başlatma, autocompact uyumu, selftest/report, karar katmanı (öğrenen yönlendirici, Fable ETA, git), uptime katmanı (headless çıkarımı, ölü hook, öğrenilen tavan, ETA), kuyruk modu ve lite yazma politikası, alt-ajan kapısı (eşikte ret / altında sessiz), uyarlanır uyarı bandı, sıkıştırma öncesi erken durma (bağlam %90 vs %30 karşılaştırmalı), sıkıştırma özetli checkpoint, `/clear` devri (yarım bekleme serbest, park edilen korunur, model taşındı), bağlam farkındalı yönlendirme (kodlama oturumunda araştır → Fable, sessiz oturumda → lite, web sinyali her durumda → lite), özet/uzun metin ayrımı, talimat uzunluğu, paralel statusLine kilidi, saat kayması düzeltmesi, hooks.json öz-onarımı, öz-denetim (bir kez/gün), yetim checkpoint bağlamı, eşik altı sessizlik + hot path süresi, uyarı bandı (bir kez), ani yükseliş öngörüsü, hook içi bekleme (kalp atışı, izleyici iptali, checkpoint tüketimi), öldürülen hook → runner kurtarması (hesap/ortam/effort doğrulaması), haftalık uzun bekleme + prompt kuyruğu + izleyici değişimi, Fable akışı (OAuth başlıkları, ayar geçişi, devir kilidi, ortam işareti, kota sonrası geri dönüş), bayat veri → API yedeği, kenar tazeleme, 429 suçlu tespiti + Fable şüphesi + dahili devam iptali, 22 TR/EN sınıflandırma örneği + ret/izin/üst sınır, iki hesap izolasyonu, 12 paralel yazım, 300 KB transcript checkpoint'i, budama, oturum sonu temizliği, config hatası bildirimi, errors.log içeriği, doctor, kurulum/kaldırma. Önceki bash regresyonundan taşınan kontroller: 17 sınıflandırma örneği (Türkçe ekler dahil), yönlendirme çıktısı + PreToolUse ret/izin/üst sınır + alt-ajan muafiyeti, oturum sonu temizliği, errors.log içeriği, kalp atışı ve runner yeniden kontrolü, ayrıca v1'den:
statusLine yakalama, eşik altı sessizlik, hook içi bekleme (süre + temizlik + izleyici iptali), haftalık durdurma + prompt kuyruğu + tek sleeper, skill muafiyeti, cancel, SessionStart işaretçisi (bir kez), PostModelSwitch, Fable prompt bloğu + settings geçişi, Fable batch yeniden başlatma, runner devir kilidi (eski pencere bloklu, yeni pencere serbest), devam argümanları/ortam değişkenleri, `__proto__` sertleştirme, StopFailure suçlu pencere; ayrıca birim: OAuth payload ayrıştırma (limits[] / düz / çöp), evaluate, tırnaklama, süre/hash; runner dalları (hâlâ limitli → yeniden planla, transcript değişmiş → atla, kuyruk prompt'u ile başlat, veri yok → tekrar dene/vazgeç, mode none), 12 paralel yazım, kesilen bekleme, zincir statusLine, off/on/model/checkpoint, kurulum (iki hesap, yabancı statusLine zinciri, bozuk settings reddi, kaldırma, iç içe kaynak koruması).

Windows'ta elle doğrulanacaklar: (1) durum çubuğunda `🛡` satırı; (2) `noctis doctor` tüm satırlar OK; (3) `thresholds.session5h`'i geçici olarak mevcut kullanımın altına çekip bir prompt gönderin → hook içi bekleme ve reset sonrası devam; (4) Görev Zamanlayıcı'da `Noctis-*` görevinin oluşup silindiği; (5) `notify.ps1`'i elle çalıştırıp tek sesli toast (odaklanma yardımı açıkken sessiz); (6) `launch.ps1` ile yeni pencere ve `NOCTIS_HANDOFF` değişkeni.
